# SNS 구현 명세

기준 문서: `docs/assignment.md`에 보존한 8around 원본 README. 원문과 충돌하면 원문을 우선한다. 이 문서의 세부 정책은 구현 결정이다. 아래 파일·명령·기능은 구현 대상이며, 실제 실행 전에는 완료로 기록하지 않는다.

## 1. 완성할 결과

외부 방문자가 가입하고 로그인하여 글을 작성한다. 별도 계정이 같은 글을 읽고 댓글을 달며, 작성자가 답글을 단다. 다른 사용자를 팔로우하고 양쪽 관계 목록을 확인한다. 동일한 소스로 로컬 앱 실행, 공개 배포, 반복 가능한 검사를 제공한다.

| 기능 | 구현 계약 |
|---|---|
| 가입·로그인 | Supabase Auth 이메일·비밀번호 인증. 신규 가입, 로그인, 로그아웃, 새로고침·페이지 이동 후 유효 세션 유지. |
| 프로필 | 가입 후 사용자명·표시 이름·소개를 입력한다. 미완료 계정은 프로필 단계로 복귀한다. 계정을 다시 생성하지 않는다. |
| 게시글 | 텍스트 작성, 전체 최신순 피드, 글 상세, 작성자 프로필 연결. |
| 댓글·답글 | 게시글의 최상위 댓글과 그 댓글에 대한 한 단계 답글. 부모·자식 관계를 저장하고 표시한다. |
| 팔로우 | 단방향 팔로우·해제, 팔로워 목록·팔로잉 목록과 각각의 수. 목록과 수는 같은 관계 데이터를 읽는다. |
| UI | 반응형 화면, 빈 상태, 입력 오류, 처리 중, 성공, 통신 오류. 실제로 없는 기능의 버튼은 표시하지 않는다. |
| 제출 | Public GitHub, 공개 배포 URL, README, tool.md, 실제 AI 기록 exports/, 소스와 검증 기록. |

### 제품 정책

- 공개 피드·프로필·댓글은 누구나 읽는다. 쓰기와 팔로우에는 로그인 및 프로필 완료가 필요하다.
- 이메일 확인 메일은 사용하지 않는다. 익명 계정 기능은 사용하지 않는다. 실제 비밀번호 인증은 유지한다. 이메일 소유권 확인과 메일 기반 계정 복구는 제공하지 않으며 가입 화면·README에 이를 밝힌다.
- 사용자명은 소문자 영문·숫자·밑줄 3~20자, 고유값이다. 표시 이름은 1~30 코드 포인트, 소개는 160 이내다. 초기 프로필 설정 이후 편집은 제공하지 않는다.
- 글·댓글은 앞뒤 공백을 정리한 일반 텍스트, 1~500 Unicode 코드 포인트다. 줄바꿈을 보존한다. 화면·서버에서 동일한 검증 함수를 사용하고 SQL에서도 공백 본문과 한도 초과를 막는다.
- 피드는 `created_at DESC, id DESC`, 20개씩 ‘더 보기’로 읽는다. offset 페이지 방식을 사용하며 새 작성·새로고침 시 첫 페이지부터 읽는다. 읽는 중 삽입에 대한 완전한 시점 일관성은 보장하지 않는다.
- 사람·팔로워·팔로잉 목록도 페이지를 나누고 더 보기를 제공한다. 댓글은 최상위 댓글을 나누어 읽고, 해당 부모 아래의 답글을 별도로 펼쳐 읽는다. 답글만 부모 없이 표시하지 않는다.
- 최상위 댓글의 깊이는 0, 답글은 1이다. 답글의 자식 생성은 거절한다. 댓글 수는 답글을 포함한다. 원문은 최대 깊이를 지정하지 않았으므로 이 제한은 선택한 정책으로 문서화한다.
- 프로필·글·댓글 삭제/수정, 파일 업로드, 좋아요, DM, 알림, 추천, 실시간 전송, 다크모드, 소셜 로그인은 구현하지 않는다.

## 2. 실행 구성

| 대상 | 구성 |
|---|---|
| 개발 | GitHub Codespaces 한 대. Codex CLI, 앱 개발 서버, 패키지 설치, 필요한 검사 실행. |
| 데이터 | 과제 전용 Supabase Cloud 프로젝트 한 개. Auth와 PostgreSQL 사용. 다른 서비스의 DB를 재사용하지 않는다. |
| 공개 | Netlify 사이트 한 개, Next.js 지원 어댑터 기본 구성. 배포된 앱이 Codespaces를 호출하지 않는다. |
| 자동 검사 | GitHub Actions 워크플로 하나. Docker 없이 정적 검사·단위 검사·빌드만 실행한다. |
| 로컬 경로 | Node·Git·npm으로 Next.js만 실행하고 원격 Supabase에 연결한다. README에 이를 명시한다. |

Next.js App Router + TypeScript + Tailwind CSS + shadcn/ui의 Radix 기반 부품 + Lucide를 사용한다. 런타임은 Node 24 LTS, 패키지 관리자는 npm이다. 이미 호환되는 앱이 있다면 보존한다. 새 앱은 지원 중인 안정 버전으로 초기화하고 실제 버전과 `package-lock.json`을 기록한다. 프레임워크 교체·실험 기능 도입은 하지 않는다.

앱은 하나의 저장소에 둔다. `main` 한 브랜치를 기준으로 작은 변경을 기록한다. 별도 release 브랜치·다중 배포 파이프라인·ORM·전역 상태 라이브러리·캐시 서버·범용 repository 계층은 만들지 않는다.

개발 앱과 제출 앱은 같은 과제 전용 DB를 사용한다. 이것은 환경 격리를 제공하지 않는 선택이다. 따라서 자동 초기화·삭제 테스트·부하 테스트를 금지한다. 신규 구조 변경은 파일로 검토하고 추가형으로 적용한다. 실제 사용자 데이터가 생긴 뒤 파괴적 변경은 중단한다. 이미 별도 환경이 구성되어 있다면 삭제하거나 합치지 않고 현재 연결을 먼저 확인한다.

Free 플랜과 기존 허용 사용량 안에서만 작업한다. 비용·유료 전환·한도 우회는 승인하지 않는다. 작업 후 Codespace를 중지한다. 무료 호스팅의 한도·일시정지를 무중단 보장으로 표현하지 않는다.

### 설정

앱의 `.env.example`에는 아래 이름과 비밀 없는 예시만 둔다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY
```

실제 값은 Codespaces의 `.env.local`과 Netlify 환경 설정에 넣는다. URL·publishable key는 공개 클라이언트용 정보이며 권한 보호는 RLS가 담당한다. DB 비밀번호·service_role·secret key는 앱과 일반 Codex 작업 환경에 넣지 않는다.

Supabase 설정은 신규 가입 허용, 이메일·비밀번호 사용, Confirm Email 비활성, 익명 로그인 비활성이다. 기본 Auth 요청 제한을 완화하지 않고 실제 가입·로그인에서 제한 오류를 처리한다. 추가 CAPTCHA·메일·도메인 서비스는 기본 의존성으로 넣지 않는다. 공개 운영의 남용 방어·계정 복구 한계는 README에 남긴다.

## 3. 화면과 요청 처리

| 경로 | 화면의 역할 |
|---|---|
| `/` | 글 작성 영역과 최신 피드. 비로그인 시 읽기와 가입·로그인 안내. |
| `/signup`, `/login` | 신규 가입, 로그인, 오류 안내. 비밀번호를 로그에 남기지 않는다. |
| `/onboarding` | 공개 프로필 최초 설정. 사용자명 충돌 시 이 단계만 재시도. |
| `/posts/[id]` | 본문, 최상위 댓글 입력, 댓글·답글 보기, 답글 대상·취소. |
| `/people` | 프로필을 찾고 방문하는 작은 사용자 목록. |
| `/u/[username]` | 소개, 해당 사용자의 글, 팔로우 버튼. `tab=followers`와 `tab=following`으로 두 목록 제공. |

조회는 Server Component에서 필요한 공개 필드만 가져온다. 쓰기는 기능별 Server Action으로 모은다. 폼·상태 전환만 Client Component로 만든다. 동일한 기능에 REST API를 중복 작성하지 않는다.

인증은 `@supabase/ssr`의 공식 Next.js 구성을 따른다. 요청별 서버 클라이언트와 쿠키 갱신을 구성한다. 보호된 쓰기에서 `getUser()`로 사용자를 확인하며, `getSession()`의 사용자 객체만 신뢰하지 않는다. 설치한 버전에 맞는 proxy/middleware 파일명을 사용한다.

쓰기 순서: 사용자 확인 → 입력 검사 → 필요한 대상 조회 → RLS가 적용된 저장 → 영향받은 경로 재조회 → 실제 결과 표시. 클라이언트에서 보낸 작성자·작성 시각·권한 항목은 허용하지 않는다. Server Action의 출처 보호를 비활성화하지 않는다.

세션과 개인별 UI 상태를 공용 캐시에 넣지 않는다. DB 조회 페이지는 빌드 시 DB에 접속하지 않도록 동적 처리한다. 빌드 통과를 위해 런타임의 설정 누락을 빈 목록이나 가짜 데이터로 숨기지 않는다.

## 4. DB 구조와 접근 규칙

`supabase/migrations/`에 초기 SQL을 작성한다. 소유자가 확인한 신규 과제 프로젝트의 SQL Editor에서 같은 파일을 한 번 적용하는 것을 기본 경로로 한다. 재현 가능한 실제 적용 순서와 후속 변경을 README에 적는다. 실행하지 않은 SQL을 적용 완료로 기록하지 않는다.

| 테이블 | 주요 열·관계 |
|---|---|
| `profiles` | `id uuid PRIMARY KEY` → `auth.users.id`; `username UNIQUE`; `display_name`; `bio`; `created_at`. |
| `posts` | `id uuid PRIMARY KEY`; `author_id` → `profiles.id`; `content`; `created_at`. |
| `comments` | `id uuid PRIMARY KEY`; `post_id` → `posts.id`; `parent_id` nullable; `author_id` → `profiles.id`; `content`; `created_at`. |
| `follows` | `follower_id`·`followee_id` → `profiles.id`; 두 열의 복합 기본키; `created_at`; 자기 관계 금지. |

모든 표에 RLS를 활성화한다. `anon`·`authenticated`에 남아 있는 광범위한 테이블 쓰기 권한을 회수한 뒤 아래 권한만 부여한다. 읽기는 공개 필드에 한해 허용한다. 인증되지 않은 INSERT·DELETE는 거절한다.

- `profiles.id`, `posts.author_id`, `comments.author_id`, `follows.follower_id`의 기본값은 `auth.uid()`다. INSERT 정책에서도 현재 사용자와의 일치를 검사한다.
- INSERT 가능한 열만 grant한다: 프로필은 사용자명·표시 이름·소개, 글은 본문, 댓글은 게시글·부모·본문, 팔로우는 대상 사용자. ID·소유자·시각은 외부가 임의 지정하지 못하게 한다. 기본값만으로 입력 제한이 된다고 가정하지 않는다.
- UPDATE는 네 표 모두 일반 사용자에게 제공하지 않는다. DELETE는 `follows`에서 본인이 만든 관계의 해제만 제공한다. 실제로 제공하지 않는 쓰기 권한을 미리 열어 두지 않는다.
- 댓글에는 `(post_id, id)`의 유일성과 `(post_id, parent_id)`의 복합 외래키를 둬 같은 게시물의 부모만 허용한다. 부모 없는 루트는 허용한다.
- 댓글 INSERT 트리거에서 부모의 `parent_id IS NULL`을 검사하여 깊이를 제한한다. 다른 행을 보는 조건을 일반 CHECK로 구현하지 않는다. UPDATE가 없으므로 부모 관계는 생성 후 바뀌지 않는다.
- DB 함수는 불필요하게 만들지 않는다. 필요한 트리거는 호출자 권한으로 동작하게 하고, 사용자 쓰기를 관리자 권한 함수로 우회하지 않는다.
- 글·댓글은 DB에서도 NULL·공백 본문·500 초과를 거절한다. 한글·이모지·탭·줄바꿈을 테스트한다. 프로필 길이와 사용자명 규칙도 DB에 반영한다.
- 조회에 필요한 인덱스만 둔다: 피드의 시각·ID, 작성자별 글, 게시글·부모별 댓글, 팔로우 대상별 역방향 목록.

앱 서버를 거치지 않는 Supabase Data API 요청에서도 위 규칙이 유지되어야 한다. 이메일·비밀번호·토큰을 공개 프로필이나 응답 객체에 넣지 않는다.

## 5. 실패 처리와 화면 표현

저장 중에는 같은 폼의 반복 제출을 막는다. 성공을 받은 뒤에만 입력창을 비우고 실제 생성된 항목을 표시한다. 확정된 입력 오류는 수정 방법을 안내한다. 응답 유실은 ‘저장 결과를 확인하지 못했습니다. 목록을 새로 확인해 주세요.’로 안내하며 초안을 보존한다. 쓰기 요청을 자동으로 재전송하지 않는다.

글·댓글에는 범용 멱등성 시스템을 구현하지 않는다. 통신 결과가 불명확한 뒤 사람이 재작성하면 중복될 수 있음을 제한사항에 기록한다. 버튼 잠금을 네트워크 중복 저장 전체의 해결책으로 설명하지 않는다. 팔로우 중복은 복합 기본키로 막고, 같은 대상의 동작은 한 화면에서 차례로 처리한다.

본문은 HTML로 해석하지 않고 텍스트로 출력한다. Enter는 본문의 줄바꿈으로 사용하며, 작성은 명시적인 버튼으로 수행한다. 한글 조합 중 의도하지 않은 제출을 방지한다. 로그인 전환 시 이전 계정의 초안·대기 응답을 새 계정에 반영하지 않는다.

### 시각 기준

- 제품명은 ‘사이’. 첫 화면은 소개용 랜딩이 아니라 피드다. 큰 화면에는 작은 왼쪽 메뉴와 중앙 읽기 영역, 모바일에는 한 열과 실제 메뉴만 둔다.
- 메뉴는 ‘전체 글 / 사람들 / 내 프로필’. 게시물은 작성자·시각 → 본문 → 댓글 링크 순서로 읽힌다. 반복 그림자 카드·통계 패널·가짜 알림·과도한 그라데이션은 쓰지 않는다.
- 스타일 값은 `globals.css`에 모은다. 본문 16px, 행간 1.7, 읽기 폭 약 660px, 흰색·옅은 회색·먹색과 저채도 청록을 사용한다. 간격은 4·8·12·16·24·32px를 기준으로 한다.
- shadcn/ui의 Radix 기반에서 필요한 버튼·입력·메뉴 등의 부품만 가져와 수정한다. Lucide는 실제 행동에만 사용한다. 라이브러리 기본 데모를 앱 화면으로 사용하지 않는다.
- 기본 폰트는 시스템 폰트로 실행 가능하게 한다. Pretendard는 공식 npm/원본 배포에서 필요한 웹폰트를 가져와 자체 제공한다. 외부 폰트 요청 실패로 빌드가 막히지 않게 한다.
- 외부 코드·아이콘·폰트의 채택 버전과 라이선스·저작권 고지를 보존한다. `THIRD_PARTY_NOTICES.md`에 사용 범위를 기록한다.
- 답글을 열면 입력에 포커스를 보내고 대상 이름·취소를 표시한다. 처리 중 표시, 입력 가까운 오류, 빈 목록의 다음 행동을 구현한다. 포커스 표시와 레이블, 동작 감소 설정을 유지한다.
- 390px·1440px에서 긴 한국어 글, URL, 긴 이름, 빈 피드, 여러 답글을 직접 확인한다. 사용자가 쓴 데이터 대신 가상 목록을 성공 결과처럼 보여 주지 않는다.

## 6. 코드와 최소 하네스

다음은 역할별 배치다. 프레임워크의 필수 설정 파일은 추가하되, 파일 수를 맞추기 위한 계층은 만들지 않는다.

```text
src/app/                   페이지, 전역 스타일, 오류·로딩 화면
src/components/            작성 폼, 글 행, 댓글, 팔로우, 공통 UI
src/lib/supabase/           브라우저·서버 클라이언트와 세션 갱신
src/lib/validation.ts      입력 규칙과 상수
src/actions/               auth.ts, posts.ts, comments.ts, follows.ts
supabase/migrations/       실제 DB 구조·권한 변경 파일
tests/validation.test.ts   본문·프로필·요청 형태의 단위 검사
scripts/check-access.mjs   실제 일반 사용자·익명 접근 검사
.github/workflows/ci.yml   정적 검사·단위 검사·빌드
AGENTS.md                  작업 규칙과 확인 명령
.env.example               앱 설정 예시
README.md                  원문이 요구한 여섯 내용
IMPLEMENTATION_PLAN.md     구현 계약
THIRD_PARTY_NOTICES.md      사용 출처·라이선스
tool.md                    실제 사용한 AI 도구와 용도
exports/                   비밀을 정리한 실제 대화 내보내기
docs/assignment.md         변경하지 않은 과제 원문
docs/verification.md       실행 결과·미검증·배포 식별
```

`node_modules/`, `.next/`, 실제 `.env*`, `.codex/`, 원시 미검토 AI 로그, 인증 상태 파일은 Git에서 제외한다. `.env.example`과 잠금 파일, migration, 검토한 `exports/`는 추적한다. 원시 기록은 Git 제외 경로에 보관한다. 비밀을 정리할 때 실행 결과를 재작성하지 않는다.

| 명령 | 실제 수행할 일 |
|---|---|
| `npm run dev` | Next.js 시작. 데이터 삭제나 샘플 생성을 하지 않는다. |
| `npm run check` | Prettier 확인 → ESLint → TypeScript → Vitest 단위 검사. |
| `npm run build` | Next.js 배포용 빌드. 실DB 접근을 요구하지 않는다. |
| `npm run test:access` | 아래의 실제 접근 검사. 대상 확인 뒤에만 실행한다. |

CI는 표준 Linux 러너 한 작업에서 `npm ci`, `npm run check`, `npm run build`를 수행한다. 실제 DB·Codex 자격 증명을 주지 않는다. 빌드에 필요한 URL·공개키 형식의 값은 테스트용 값을 사용하고 DB 네트워크 호출은 하지 않는다. CI 성공을 실제 DB 검사의 성공으로 표시하지 않는다.

### 실제 접근 검사

`check-access.mjs`는 관리 키 없이 Supabase SDK로 독립된 점검 계정 A·B에 로그인한다. 사용자 ID가 다르고 프로필이 완료됐는지 확인한다. URL·publishable key·점검 계정 자격 증명은 Git에서 제외한 `.env.test.local`로 읽는다. 별도 `ACCESS_CHECK_PROJECT_REF`가 URL의 프로젝트 ID와 일치해야 시작한다. 환경값·이메일·토큰을 출력하지 않는다.

정상 글·댓글에 `[점검]` 표시와 실행 식별자를 붙여 소량 생성한다. reset, 사용자 삭제, 다른 글 수정·삭제는 하지 않는다. 실행 후 생성한 점검 자료의 범위를 보고하고 실패하면 종료 코드를 0이 아닌 값으로 반환한다. 공개 제출 후 반복 자동 실행하지 않는다.

검사 항목: 익명 쓰기 거절, A 정상 작성·B 조회, 작성자 B 사칭 필드 거절, 자기 팔로우 거절, 같은 A→B 반복 시 한 관계, B의 A→B 해제 차단, 정상 C1·C2 생성, 다른 글의 부모·깊이 초과 거절. 응답 오류 여부뿐 아니라 실제 반환 행·관계 수를 확인한다.

## 7. 구현 순서

| 단계 | 작업 | 확인하고 남길 결과 |
|---|---|---|
| P0 | 기존 파일 확인, 앱 초기화, 설정 예시·Git 제외 규칙·짧은 AGENTS·AI 기록 마련. 최소 화면을 빌드하고 Netlify 연결. | 코드 실행, 실제 빌드 결과, 첫 외부 URL 또는 필요한 계정 작업. |
| P1 | 초기 SQL·RLS 작성. 승인된 프로젝트에 적용. 가입·로그인·프로필·글 작성·피드 연결. 공통 입력 검사와 단위 검사도 함께 작성. | 신규 A 가입·로그인, B의 P1 조회, 새로고침 후 유지. |
| P2 | 프로필·사람 목록, 팔로우·해제·두 목록, 댓글·답글 연결. | A→B 목록 일치, P1 아래 C1·C2 저장·조회. |
| P3 | 같은 컴포넌트를 실제 데이터로 다듬기. 모바일·키보드·실패 표시, 실제 접근 검사, CI 구성. | check·build·test:access 실제 결과와 화면 확인. |
| P4 | 배포 갱신, 외부 A/B 체험, 문서·라이선스·실제 대화 기록 정리. | 원문 전체 흐름과 제출 파일의 일치. |

단계마다 새 계획서를 만들지 않는다. 기존 결과를 보존하면서 구현·검사·수정한다. 허용된 저장소 내부 작업은 이어서 수행한다. 외부 계정 작업, DB 적용, 공개 push·배포 등 필요한 승인 경계에서만 대상과 행동을 구체적으로 요청한다.

차단 요인이 있으면 필요한 작업을 한 번에 보고하고 독립적으로 가능한 코드를 계속 구현한다. 연결 실패를 가짜 로그인·가짜 저장·RLS 비활성화로 대체하지 않는다. 범위 밖 기능은 추가하지 않는다.

## 8. 배포와 문서

Netlify는 해당 저장소의 `main`을 사용한다. 자동 프레임워크 감지를 확인하고 빌드 명령은 `npm run check && npm run build`로 설정한다. 정적 HTML export로 Server Actions를 없애지 않는다. 앱 설정 두 값을 넣고 실제 런타임에 전달되는지 검사한다. 무관한 PR의 자동 배포·별도 preview 환경 구축은 기본 범위에 넣지 않는다.

프로젝트 소유자는 Supabase SQL 적용과 Auth 설정, Netlify 연결·환경 설정을 수행한다. Codex는 적용할 파일과 정확한 설정 위치를 안내한다. 위임된 권한이 없는 계정 작업을 수행했다고 보고하지 않는다.

공개 체험용 가상 계정 두 개에 짧고 긴 한국어 글·댓글을 소량 직접 작성한다. 예시 계정임을 소개에 표시한다. 이 자료는 첫 화면을 이해시키기 위한 것으로, 심사자의 신규 가입을 대신하지 않는다. 계정 비밀번호는 README에 게시하지 않는다.

README의 필수 구성:
1. 프로젝트 목적, 실제 배포 URL, 기능과 짧은 체험 순서.
2. 로컬 실행: Node 24·Git → `npm ci` → 원격 Supabase 신규 프로젝트와 SQL 적용 → `.env.local` → `npm run dev`. 기존 배포 DB와 신규 재현 프로젝트를 구분한다.
3. 기술 스택 선택 이유: 한 앱, 관리형 인증·DB, 필요한 UI 부품 재사용.
4. 하네스: AGENTS, 입력 검사, DB migration·RLS, check·build·test:access, CI가 각각 확인하는 것.
5. 실제 의사결정과 비용: 한 DB 공유, 한 단계 답글, 확인 메일·수정/삭제 제외, 수동 전체 흐름 확인, 일반적인 재시도 중복 가능성.
6. 개선할 점: 계정 복구·이메일 확인, 남용 방어, 환경 분리, 브라우저 자동 회귀 검사. 미완료 필수를 선택 개선으로 숨기지 않는다.

`docs/verification.md`에는 시각, 코드 commit, DB 적용 파일, 배포 URL·식별, 실행 명령·결과, 실제 수동 확인·미검증을 기록한다. 데스크톱·모바일 화면 이미지를 넣는다면 실제 배포 화면만 사용한다. 가상 시안을 실행 증거로 사용하지 않는다.

## 9. Codex 기록과 종료 조건

작업 시작부터 실제 입력·응답·도구 결과를 보관한다. 설치한 Codex의 transcript export가 있으면 우선 사용한다. 없는 경우 `codex exec --json` 이벤트와 입력 원문·stderr·종료 상태를 보존한다. `-o`의 마지막 답변만 전체 대화로 제출하지 않는다. 대안 형식의 원문 조건 충족 여부는 제출 확인사항으로 표시하고, 기록 보존과 앱 구현은 계속한다.

`exports/`에는 검토·정리한 실제 내보내기만 넣는다. 수작업 예시·빈 파일·사후 생성 요약을 대화로 위장하지 않는다. `tool.md`에는 Codex와 실제 사용한 AI 관련 구성의 용도만 적는다. 사용하지 않은 MCP·플러그인을 목록에 추가하지 않는다.

작업 보고는 **변경 파일 / 실제 실행 결과 / 차단·미검증 / 다음 작업**으로 끝낸다. 불필요한 설명 문서와 일반적인 기술 해설은 생성하지 않는다.

완료 확인:
- 외부 방문자가 신규 가입 → 로그인 → 글 작성 → 댓글·답글 → 팔로우를 끝까지 수행했다.
- 독립된 A·B 세션에서 글·관계·계층이 유지되고 로그아웃 후 쓰기가 거절된다.
- 실제 접근 검사에서 사칭·잘못된 관계가 저장되지 않는다.
- check와 build가 통과하고, CI와 수동 검사가 증명한 범위를 구분했다.
- 문서대로 신규 환경에서 실행했거나, 아직 못 한 부분을 미검증으로 표시했다.
- Public 저장소와 공개 URL, README 여섯 내용, tool.md, 실제 exports/가 있다.
- 제출 이메일에는 두 링크를 포함한다. 원문의 발송 시각·저장소 상태 기준을 따른다. 코드 push만으로 제출 완료라 하지 않는다. 이메일 발송은 소유자가 수행한다.

### 구현 중 필요한 공식 문서

- Next.js 설치: https://nextjs.org/docs/app/getting-started/installation
- Supabase SSR·사용자 검증: https://supabase.com/docs/guides/auth/server-side/creating-a-client
- Supabase 접근 정책: https://supabase.com/docs/guides/database/postgres/row-level-security
- Supabase 인증 설정: https://supabase.com/docs/guides/auth/general-configuration
- shadcn CLI: https://ui.shadcn.com/docs/cli
- Netlify Next.js: https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/
- Codex 실행 기록: https://developers.openai.com/codex/noninteractive/

현재 작업에 필요한 문서만 확인한다. 공식 문서는 구현 방법의 근거이며 원문 요구를 바꾸는 근거로 사용하지 않는다.
