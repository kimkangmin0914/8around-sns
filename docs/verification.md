# P0/P1/P2/P3/P3.5 실행 기록

## 1. 변경 파일

2026-09-17 UTC, 기존 `main` 작업 트리에서 구현했습니다. 기존 `AGENTS.md`, `IMPLEMENTATION_PLAN.md`, `docs/assignment.md`와 사용자 `.env.local`은 보존했습니다. 구현 시작 기준 commit은 `7239402`이며, P0~P3 구현은 `ee0c4600d55ccc26d9693f8c605b1997d172c868`으로 commit·push했습니다. 이후 문서·실제 대화 스냅샷 갱신은 별도 commit으로 기록합니다.

- 앱·설정: `package.json`, `package-lock.json`, Next.js/TypeScript/Tailwind/ESLint/Vitest 설정, `.nvmrc`, `.env.example`, `.gitignore`, `netlify.toml`.
- P0/P1 코드: `src/app/`, `src/components/`, `src/lib/`, `src/actions/`, `src/proxy.ts`, `public/fonts/`.
- P2 코드: `src/actions/comments.ts`, `src/actions/follows.ts`, `src/lib/comments.ts`, `src/lib/comment-page.ts`, `src/lib/people.ts`, `src/app/people/page.tsx`, `src/app/u/[username]/page.tsx`, 댓글·팔로우·사람·메뉴 컴포넌트. 기존 게시글·상세·레이아웃·DB 타입·입력 검사를 확장했습니다.
- P2 단위 검사: `tests/social-actions.test.ts`, `tests/social-reads.test.ts`, `tests/comment-page.test.ts`; 기존 게시글 검사에 프로필 경로 갱신을 추가했습니다.
- P3 코드·검사: `scripts/check-access.mjs`, `tests/access-settings.test.ts`, `.github/workflows/ci.yml`, `package.json`; `src/lib/auth-session.ts`, `src/lib/viewer.ts`, `src/components/session-boundary.tsx`, `src/components/comments-section.tsx`; 이미지 첨부 비밀 제거를 추가한 `scripts/export-session.mjs`.
- DB 초안: `supabase/migrations/202609170001_initial.sql`.
- 단위 검사: `tests/validation.test.ts`, `tests/posts-action.test.ts`, `tests/viewer.test.ts`, `tests/multipart-validation.test.ts`.
- 문서·실제 기록: `README.md`, `tool.md`, `THIRD_PARTY_NOTICES.md`, `scripts/export-session.mjs`, `exports/codex-session.jsonl`, 이 파일. 원본은 Git 제외 `.ai-raw/`에 보존했습니다.

## 2. 실제 실행한 검사와 결과

| 시점 (UTC, 2026-09-17) | 증거 종류      | 실행·결과                                                                                                                                                                                                                                                                 |
| ---------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 06:10 이후             | 저장소 확인    | 문서 4개와 미추적 `.env.local` 확인. 기존 앱 소스 없음. Node 24.20.0, npm 11.19.0.                                                                                                                                                                                        |
| P0                     | Git 제외       | `git check-ignore`로 `.env.local`, 원시 AI 기록, `.codex/auth.json` 제외 확인. 인증 파일 내용은 읽지 않음.                                                                                                                                                                |
| P0                     | 의존성         | npm 설치 성공, audit에서 알려진 취약점 0 보고. 처음 DNS 제한으로 실패하여 승인된 설치로 진행.                                                                                                                                                                             |
| 06:18                  | 정적·단위      | P0 `npm run check`: 23개 단위 검사 통과. 초기 설정 경고는 후속 수정.                                                                                                                                                                                                      |
| P0                     | 빌드 실패·수정 | Turbopack CSS 처리의 내부 포트 바인딩이 권한 제한으로 실패. 승인된 재실행도 같은 오류. Next.js `--webpack`으로 변경한 `npm run build` 성공, `/` 최소 화면 정적 빌드.                                                                                                      |
| P1 초기                | 원격 읽기만    | 런타임이 `.env.local`을 읽어 Supabase 공개 설정·테이블 존재 조회. Auth HTTP 200, 신규 가입·Email 로그인·Confirm Email 비활성 확인. `profiles`, `posts`는 HTTP 404. 계정 생성·SQL 적용·자료 추가 없음.                                                                     |
| P1                     | 검사 도구 호환 | ESLint 10.10.0의 React 규칙이 `getFilename` 오류로 실패. 9.39.5로 고정 후 정상 검사. 지원 종료 경고는 남음.                                                                                                                                                               |
| 06:37                  | 정적·단위      | `npm run check`: Prettier·ESLint·TypeScript·Vitest 통과, 2개 파일 33개 검사 성공.                                                                                                                                                                                         |
| 06:38 이후             | 빌드           | P1 `npm run build` 성공. `/`, `/login`, `/signup`, `/onboarding`, `/posts/[id]` 동적 렌더링, Proxy 포함. SQL 미적용·네트워크 제한 상태에서도 빌드 성공.                                                                                                                   |
| 06:42                  | 정적·단위      | 만료 세션 분기 수정 후 `npm run check` 통과, 3개 파일 38개 검사 성공. 로그인이 필요한 상태와 통신 오류를 구분.                                                                                                                                                            |
| 06:42 이후             | 로컬 HTTP      | `npm run start`는 기본 샌드박스에서 EPERM, 승인된 실행에서 성공. `/`, `/login`, `/signup` HTTP 200 및 private/no-store 확인. DB 미적용 피드는 오류 표시이며 빈 목록 성공으로 표시하지 않음. 가입/로그인 입력과 계정 복구 미제공 고지 확인.                                |
| 06:43 이후             | 로컬 HTTP      | `/onboarding`과 잘못된 글 ID에서 307/404 상태를 기대한 초기 단언은 실패. Next.js 스트리밍으로 실제 상태는 200. 응답 파일 검사에서 로그인 이동 `meta refresh`와 없는 글 화면·`noindex` 확인. 자체 WOFF2 HTTP 200. 브라우저에서 실제 이동한 증거는 아님. 점검 후 서버 종료. |

최종 확인 (06:46~06:48 UTC): `npm run check`에서 정적 검사와 38개 단위 검사가 통과했습니다. 이어진 빌드는 TypeScript `--showConfig` 출력 수집 오류로 한 번 실패했습니다. 직접 CLI 설정 출력은 정상이었고 Next.js 하위 프로세스 진단에서는 출력이 비어 있는 경우를 확인했습니다. 동일 코드의 `npm run build` 재실행은 종료 코드 0으로 통과했습니다. 진행 중 추가로 실행한 빌드 한 건은 동시 실행 잠금으로 종료됐습니다. 최종 빌드에는 만료 세션 수정과 다섯 동적 페이지·Proxy가 포함됩니다.

`git diff --check` 통과, 원문·계획·AGENTS 변경 없음, 실제 환경·원시 AI 기록의 Git 제외를 확인했습니다. 실제 대화 내보내기의 설정값 포함 여부와 JWT/키 패턴 검사에서는 노출을 발견하지 못했습니다. 내보내기 시각·레코드 수·원본 해시는 해당 JSONL 첫 행에 기록됩니다.

단위 검사에서 SDK 모의 응답을 사용하는 것은 Server Action 분기 검증입니다. 이를 실제 DB RLS 검사나 실제 사용자 저장 성공으로 기록하지 않습니다. 검사를 통과시키기 위한 RLS 비활성화·가짜 런타임 인증·가짜 저장은 없습니다.

### SQL 적용 확인 후 점검 (2026-09-17, 07:20~07:28 UTC)

- 소유자가 과제 전용 프로젝트의 `202609170001_initial.sql` 적용과 익명 로그인 비활성을 확인했습니다. 에이전트가 SQL을 재실행하거나 Auth 설정을 변경하지 않았습니다.
- 공개 Auth 설정 HTTP 200에서 신규 가입·Email 로그인 활성, Confirm Email 비활성을 확인했습니다. 익명 로그인 설정은 공개 응답에 노출되지 않아 소유자 확인을 근거로 기록합니다.
- `profiles`, `posts`, `comments`, `follows`의 공개 조회가 모두 HTTP 200이었습니다. 게시글 조회에 `profiles!posts_author_id_fkey` 조인을 포함해 작성자 연결도 확인했습니다. 조회는 `limit=0`이며 실제 사용자 행 내용을 로그에 출력하지 않았습니다.
- `npm run start` 후 로컬 `/` HTTP 200. 이전의 연결 오류가 사라지고 실제 빈 피드가 표시됨을 HTML 단언으로 확인했습니다.
- 인증 쿠키 없이 실제 `createPost` Server Action을 HTTP로 호출했습니다. 반환 결과는 `status: error`와 로그인 요구 안내였으며 성공으로 처리되지 않았습니다. 이는 앱의 비로그인 쓰기 경계 확인이며 Data API 직접 호출의 RLS 검사와 구분합니다. 점검 계정 생성·정상 게시글 추가·`npm run test:access` 실행은 하지 않았습니다.
- 빌드 manifest를 진단하면서 로컬 Server Action 내부 암호화 키가 도구 출력에 포함됐습니다. 내보내기 스크립트에 중첩 JSON의 해당 키 제거를 추가하고 세 가지 인코딩 깊이를 점검했습니다. 로컬 앱을 중지한 뒤 재빌드로 키를 교체했고, 이전 키와 새 키의 해시가 다름을 값 출력 없이 확인했습니다. 원시 기록은 Git 제외 경로에 보존하고 제출용 기록에서는 키를 제거했습니다. 공개 배포는 없었습니다.
- `npm run check`: Prettier·ESLint·TypeScript 및 38개 단위 검사 통과. `npm run build`: 첫 시도에서 기존 TypeScript 출력 수집 오류가 재발했으나, 승인된 재실행은 종료 코드 0으로 통과했습니다. 점검 후 로컬 서버를 종료했습니다.
- 이 시점에는 `.env.test.local`이 없어 점검 계정 준비 방식을 사용자에게 확인 중이었습니다. 이후 승인 및 P1 실제 검증 결과는 아래에 이어서 기록합니다.

### 승인된 신규 A·B 계정의 P1 검증 (2026-09-17, 07:29~07:38 UTC)

소유자는 과제 전용 프로젝트에 점검 계정 A·B를 각각 하나씩 생성하고 자격 증명을 Git 제외 파일에만 보관하도록 승인했습니다. `.env.local`을 런타임에서 읽어 동일 프로젝트를 확인했으며 `.env.test.local`에 URL·publishable key·별도 프로젝트 참조·두 계정 자격 증명을 보관했습니다. 값은 출력하지 않았고 파일 권한 `600` 및 Git 제외를 확인했습니다. 실제 점검 스크립트·진행 상태는 `.ai-raw/`에 보존했습니다.

| 증거 종류        | 실제 확인 결과                                                                                                                                                                                                                                                                |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 실제 앱 HTTP     | 설치된 React Flight 인코더로 실제 Server Action 요청을 전송했습니다. 앱의 `signUp`을 통해 A·B 두 계정을 생성했고 응답 쿠키와 `getUser()`로 서로 다른 실제 사용자를 확인했습니다.                                                                                              |
| 실제 앱 HTTP·DB  | 미완료 계정의 프로필 단계 복귀, A 프로필 저장, B 사용자명 충돌의 입력 오류, 같은 B 계정으로 다른 사용자명을 저장하는 흐름 통과.                                                                                                                                               |
| 실패 재현·수정   | 첫 500자 게시글은 입력 오류로 거절됐습니다. Multipart 전송에서 LF 두 개가 CRLF로 바뀌어 서버에서 502자로 계산됐음을 로컬 Request 인코딩·디코딩으로 재현했습니다. 공통 검증과 표시에서 CRLF/CR을 LF로 통일했고 소개에도 같은 규칙을 적용했습니다. SQL·RLS를 바꾸지 않았습니다. |
| 정적·단위        | 수정 후 `npm run check`에서 Prettier·ESLint·TypeScript와 4개 파일 42개 검사가 통과했습니다. 새 4개 검사는 실제 multipart 전송의 500자 허용·501자 거절·160자 소개·CR 줄바꿈을 검증합니다.                                                                                      |
| 빌드             | 수정 후 `npm run build` 종료 코드 0. 동일한 다섯 동적 경로와 Proxy 포함.                                                                                                                                                                                                      |
| 실제 앱 HTTP·DB  | 기존 A·B로 로그인하여 계속했습니다. A가 한글·이모지·줄바꿈을 포함한 정확히 500 코드 포인트의 게시글 1개를 앱에서 저장했습니다. B의 인증된 Data API 조회로 동일한 ID·작성자·본문을 확인했습니다.                                                                               |
| 실제 앱 HTTP     | A·B 각각의 쿠키를 새 요청에 복원하여 로그인 유지·피드·글 상세의 저장된 내용을 확인했습니다. 실제 브라우저 새로고침을 실행한 증거는 아닙니다.                                                                                                                                  |
| 실제 앱 HTTP     | 임의 작성자 필드와 이전 계정의 초안을 거절했습니다. A 로그아웃 후 글 작성이 거절됐고, 비밀번호 재로그인이 기존 A 계정으로 돌아오는 것을 확인했습니다.                                                                                                                         |
| 실제 Data API·DB | 익명 INSERT 및 작성자 사칭 열 INSERT가 `42501`로 거절됐습니다. 공백 본문·501자 본문은 `23514`로 거절됐습니다. 실행 표시를 가진 게시글을 다시 조회해 저장된 글이 정확히 1개임을 확인했습니다. 이는 P1 범위 검사이며 댓글·팔로우를 포함하는 P3 전체 접근 검사는 아닙니다.       |

최종 추가 자료는 Auth 계정 2개, 공개 프로필 2개, `[점검]` 게시글 1개입니다. 최초 입력 오류 후 동일 계정과 같은 실행 표시를 사용했고 저장 전 기존 행을 확인했습니다. 추가 가입·중복 글·댓글·팔로우를 만들지 않았으며 SQL 재적용·reset·수정·삭제·부하 검사를 하지 않았습니다. A·B 모두 로그아웃하고 로컬 앱 서버를 종료했습니다. 실제 계정과 점검 자료는 프로젝트에 보존했습니다.

### P2 구현과 기존 A·B 검증 (2026-09-17, 07:58~08:17 UTC)

기준 commit `7239402` 위의 미커밋 작업 트리에서 진행했습니다. 계획·기술 스택·패키지·초기 SQL은 변경하지 않았습니다. 현재 적용된 `202609170001_initial.sql`의 댓글·팔로우 제약과 RLS를 그대로 사용했습니다. 프로필과 사람 목록, 양쪽 관계 목록·수, 팔로우·해제, 댓글·답글과 별도 페이지 조회를 연결했습니다.

| 증거 종류        | 실제 실행·결과                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 정적·단위        | 단계별 `npm run check` 실행. 첫 화면 연결에서는 기존 `Viewer.status`, `validateOffset`의 반환형, Button variant와 다른 이름을 사용해 TypeScript 검사가 실패했습니다. 기존 인터페이스에 맞춰 수정한 후 통과했습니다. 최종 Prettier·ESLint·TypeScript 및 **7개 파일 79개 단위 검사 통과**. 소셜 쓰기 경계, 부모·깊이·소유자, 20개 조회와 21번째 더 보기 판단, 관계 방향, 잘못된 페이지 주소를 검사합니다. 단위 검사 SDK 응답은 모의 값이며 실제 DB 검사는 아래와 구분합니다. |
| 빌드             | `npm run build` 종료 코드 0. `/people`, `/u/[username]`, 기존 인증·피드·글 상세와 Proxy 포함. 동적 페이지로 빌드 시 DB를 조회하지 않습니다.                                                                                                                                                                                                                                                                                                                                |
| 실행 경계        | `npm run start`로 빌드된 로컬 앱 실행. `.ai-raw/check-p2.mjs`의 첫 실행은 샌드박스 연결 제한으로 로그인 요청 전 `fetch failed`가 났습니다. 승인된 동일 명령 재실행은 종료 코드 0으로 완료했습니다. 값은 런타임에만 읽고 동일 과제 프로젝트·기존 두 계정 ID·공개 프로필을 검증했습니다.                                                                                                                                                                                     |
| 실제 앱 HTTP     | 기존 A·B로 앱 `signIn`을 호출하고 각각의 응답 쿠키·`getUser()`로 별도 사용자임을 확인했습니다. 신규 계정 생성 없음. 공개 `/people`에서 A·B 프로필 링크, A 프로필에서 A의 기존 글을 확인했습니다.                                                                                                                                                                                                                                                                           |
| 실제 앱 HTTP·DB  | A→B 팔로우, 같은 원하는 상태 재요청 후 관계가 정확히 1개임을 확인했습니다. A 팔로잉·B 팔로워 HTML과 DB 집계의 방향·수가 일치했습니다. A의 해제 후 양쪽 수는 0, 다시 팔로우 후 관계 1개를 보존했습니다.                                                                                                                                                                                                                                                                     |
| 실제 Data API·DB | B가 A→B 관계를 직접 DELETE해도 반환 행 0개이고 관계가 남았습니다. 자기 팔로우는 `23514`, follower 사칭 열은 `42501`로 거절됐습니다. 앱의 follower 필드 주입도 입력 오류로 거절했습니다.                                                                                                                                                                                                                                                                                    |
| 실제 앱 HTTP·DB  | P1의 A 게시글 아래에 B가 한글·이모지·줄바꿈을 포함한 **정확히 500 코드 포인트** 댓글 C1을 저장했습니다. A는 C1을 부모로 답글 C2를 저장했습니다. 별도 B 조회에서 동일 ID·내용·작성자·글·부모를 확인했습니다. 댓글 수는 답글 포함 2개였습니다.                                                                                                                                                                                                                               |
| 실제 앱 HTTP     | 기본 상세에는 부모 C1이 보이고 C2는 접혀 있었습니다. `thread=C1`로 펼친 HTML에서 C1 아래의 답글 영역에 C2가 표시됐습니다. 루트 offset 20으로 이동한 직접 링크에서도 C1을 함께 표시해 C2만 홀로 노출하지 않았습니다. 피드의 댓글 수 2도 확인했습니다. 브라우저 클릭을 실행한 증거는 아닙니다.                                                                                                                                                                               |
| 실제 앱 HTTP·DB  | 다른 글의 부모와 답글을 부모로 지정하는 깊이 2 요청은 앱 입력 검사에서 거절됐고, 직접 Data API도 제약 오류(`23514` 또는 `23503` 허용 검사)로 거절됐습니다. 사칭 작성자 열과 익명 댓글·팔로우 INSERT는 `42501`, 공백·501자 댓글은 `23514`로 거절됐습니다. 잘못된 댓글이 실제로 0개임을 재조회했습니다.                                                                                                                                                                      |
| 실제 앱 HTTP     | 비로그인·다른 계정의 이전 폼·작성자 필드 주입을 거절했습니다. 다른 글의 `thread`와 부모 없는 `replyOffset` 주소는 오류로 표시됐고 답글만 나타나지 않았습니다. A·B 로그아웃 후 두 계정의 댓글·팔로우 요청 모두 거절됐습니다.                                                                                                                                                                                                                                                |
| Git 제외·기록    | `git check-ignore`에서 `.env.local`, `.env.test.local`, 점검 스크립트·상태가 제외됨을 확인했습니다. 자격 증명 파일과 점검 상태의 권한은 `600`입니다. 실제 요청 스크립트·실행 상태·원시 AI 기록은 `.ai-raw/`에 보존하며 제출용 `exports/`는 비밀 제거 후 실제 이벤트를 다시 내보냅니다.                                                                                                                                                                                     |

P2 추가 자료는 점검용 게시글 **1개**, 댓글 **2개**, 최종 A→B 팔로우 **1개**입니다. 전체 점검 자료는 기존 계정·프로필 각각 2개, 게시글 2개, 댓글 2개, 관계 1개입니다. 정상 해제는 A가 생성한 A→B 관계에만 수행했고, B의 타인 관계 해제 시도는 실제 영향 0개였습니다. SQL 재적용·관리 키·RLS 변경·reset·일괄 삭제·부하 검사는 없습니다. A·B 세션은 모두 로그아웃했고 로컬 점검 서버를 종료했습니다. 점검 자료는 보존했습니다.

댓글은 한 번에 루트 20개와 선택한 부모의 답글 20개를 나눠 읽습니다. 직접 링크의 부모가 현재 루트 페이지에 없으면 해당 부모 1개를 함께 표시합니다. 자기 참조 테이블의 임베딩은 방향 구분에 추가 구성이 필요하므로, 답글은 부모·글 필터로 별도 조회합니다. 추가 SQL 함수는 만들지 않았습니다. [PostgREST 공식 관계 조회 문서](https://docs.postgrest.org/en/v14/references/api/resource_embedding.html#recursive-relationships)

### P3 접근 검사·브라우저·CI 구성 (2026-09-17, 08:50~09:59 UTC)

기존 계획·스택·의존성 잠금 파일·SQL·계정·프로필을 보존했습니다. 현재 코드는 기준 commit `7239402` 위의 미커밋 작업 트리입니다. 새 `test:access`는 일반 publishable key와 준비된 A·B로만 실행되며, 별도 프로젝트 확인값·서로 다른 사용자·프로필 완료를 확인합니다. 실제 입력·SDK 응답이나 자격 증명은 출력하지 않습니다.

| 증거 종류                 | 실제 실행·결과                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 정적·단위                 | `npm run check`의 Prettier·ESLint·TypeScript와 **8개 파일 89개 단위 검사 통과**. 접근 검사 설정에서 프로젝트 불일치·관리 키·같은 계정·누락값을 거절하고 오류에 전달된 설정값을 포함하지 않는 검사를 추가했습니다. 초기 포맷 검사 실패는 정리 후 통과했습니다.                                                                                                                                                                                                                                                                     |
| 실제 Data API·DB          | `npm run test:access` 첫 실행은 샌드박스 연결 제한으로 로그인 전에 실패했고 추가 자료는 0개였습니다. 승인된 재실행은 종료 코드 0. A의 글 저장·B 조회, C1·C2의 내용·부모·작성자, 익명·작성자 사칭·공백·501자·다른 글/없는 부모·깊이 2 거절이 통과했습니다. 오류 코드뿐 아니라 거절 대상 글·댓글 0개, 정상 댓글 2개를 재조회했습니다.                                                                                                                                                                                               |
| 실제 Data API·DB          | 기존 A→B를 보존하고 반복 INSERT의 `23505` 및 관계 1개를 확인했습니다. B의 A→B DELETE는 영향 0개, 관계는 유지됐습니다. 자기 팔로우 `23514`, 팔로워 사칭·익명 팔로우 `42501`도 확인했습니다. 추가 자료는 글 2개·댓글 2개·관계 0개이며, 두 점검 세션은 로그아웃했습니다.                                                                                                                                                                                                                                                             |
| 브라우저 실행 환경        | Git 제외 `.ai-raw/browser`에 Playwright 1.63.0을 준비하고 Chromium 153.0.8010.12를 `/tmp`에 다운로드했습니다. DNS 제한, 브라우저 경로 설정, libatk 등 실행 라이브러리 누락을 확인하고 승인된 다운로드·의존성 설치로 해결했습니다. 앱의 `package-lock.json`과 배포 스택은 변경하지 않았습니다.                                                                                                                                                                                                                                     |
| 실제 Chromium             | A 1440px·B 390px 독립 컨텍스트에서 앱 로그인 폼을 조작했습니다. 실제 쿠키를 확인하고 브라우저 새로고침 뒤 같은 사용자임을 검증했습니다. A의 글을 UI로 저장한 뒤 B의 DB 조회에서 동일 본문·작성자를 확인했습니다. 긴 한국어·긴 URL이 있는 실제 피드를 두 크기에서 확인했고 가로 넘침은 없었습니다.                                                                                                                                                                                                                                 |
| 실제 Chromium             | Enter 줄바꿈, 501자 입력 오류·초안 유지, 답글 대상 표시·자동 포커스·취소, B의 추가 답글 저장을 확인했습니다. 한 부모 아래 여러 답글이 표시되고 답글에 더 깊은 쓰기 버튼이 없었습니다. 첫 표시 단언은 탐색 완료 전에 검사해 실패했고, 실제 DOM 대기를 추가한 뒤 기존 저장 자료를 재사용해 통과했습니다.                                                                                                                                                                                                                            |
| 실제 Chromium             | 팔로우·해제 버튼 조작, 새로고침 후 A 팔로잉·B 팔로워 목록 일치, 공개 사람 목록, 글 없는 B 프로필, 빈 offset 20 피드, Tab·Enter의 본문 건너뛰기와 메뉴 이동을 확인했습니다. 390px에서 답글·빈 피드·메뉴의 가로 넘침이 없었습니다.                                                                                                                                                                                                                                                                                                  |
| 실제 Chromium · 실패      | 실제 Server Action 요청을 네트워크 단계에서 중단했습니다. 글·답글은 오류와 결과 미확인을 표시하고 초안·답글 대상을 유지했으며, 화면 내 목록 재확인 후에도 유지됐습니다. 팔로우는 성공으로 바뀌지 않았습니다. 요청 자동 재전송은 없었습니다. 응답을 대기시키면 입력과 버튼이 비활성화됐고 반복 클릭에도 쓰기 요청은 1개였습니다. 중단 요청은 DB에 전달하지 않았습니다.                                                                                                                                                             |
| 실패 재현·제품 수정       | 다른 탭에서 로그아웃한 뒤 돌아오면 기존 초안이 남는 문제를 재현했습니다. `SessionBoundary`가 focus·pageshow·visibilitychange에서 `getUser()`로 다시 확인하도록 수정했습니다. 만료/없는 세션과 통신 오류를 공통 분류하며, 연결 실패만으로는 초안을 지우지 않습니다. 댓글의 모든 폼을 하나의 경계로 묶어 탭 복귀 시 폼 수만큼 확인 요청을 반복하지 않게 했습니다.                                                                                                                                                                   |
| 수정 후 실제 Chromium     | 탭 복귀 시 A→B 전환으로 A 초안이 비워지고 B가 표시됐습니다. 다른 탭 로그아웃 후 쓰기 폼이 사라졌습니다. 사용자 확인 통신 실패에서는 초안을 유지했습니다. 이전 A 쓰기 요청을 대기시킨 채 B로 전환하면 먼저 A 초안을 숨기며, 이후 도착한 실패가 B에 반영되지 않았습니다. 이 검사는 09:59:43 UTC에 종료 코드 0으로 끝났고 사용자 중단 후 결과를 회수했습니다.                                                                                                                                                                        |
| 실제 Chromium · 세션 갱신 | 실제 발급된 access/refresh token은 유지하고 쿠키의 만료 메타데이터만 과거로 조정했습니다. 앱 Proxy·Auth가 실제 새 refresh token과 만료 시각을 발급했고, 응답 쿠키 갱신·같은 A 사용자·쓰기 폼 유지를 확인했습니다. 토큰 값은 출력하지 않았습니다.                                                                                                                                                                                                                                                                                  |
| 수정 후 회귀·빌드         | 세션 수정 후 `npm run check` 89개·`npm run build` 통과. 댓글 경계 변경 뒤 글·답글·팔로우의 실패 처리도 브라우저에서 다시 통과했습니다. 모든 완료된 브라우저 검사에서 page error가 없었으며 마지막 점검 세션은 로그아웃 후 컨텍스트를 종료했습니다.                                                                                                                                                                                                                                                                                |
| CI 정의                   | `.github/workflows/ci.yml`을 YAML로 파싱해 `main` push·PR, `contents: read`, Node 24, `npm ci` → `npm run check` → `npm run build`, 실제 자격 증명 없음의 구성을 확인했습니다. checkout 7.0.1·setup-node 7.0.0의 공식 릴리즈 commit을 고정했습니다. GitHub에 push하거나 워크플로를 원격 실행하지는 않았습니다.                                                                                                                                                                                                                    |
| CI 조건의 별도 로컬 실행  | Git 추적 대상 소스만 복사하고 `.env.local`·`.env.test.local`·원시 기록을 제외한 환경에서 `npm ci`로 416개 패키지를 설치했습니다. 빌드용 `.invalid` URL·비실제 키를 사용하고 fetch를 차단한 상태로 check 89개·build가 통과했습니다. fetch 시도는 0개였습니다. 첫 별도 빌드는 기존 TypeScript `--showConfig` 출력 수집 오류로 실패했으며, 중첩 잠금 파일 경고를 제거한 `/tmp/sai-p3-ci-check`에서 승인된 재실행은 통과했습니다. 최종 앱 소스·잠금 파일·워크플로가 원본과 동일함을 비교했습니다. GitHub 러너 성공의 증거는 아닙니다. |

P3의 정상 추가 자료는 접근 검사의 글 2개·댓글 2개와 브라우저의 글 1개·답글 1개입니다. 승인된 점검 자료 누계는 계정·프로필 각각 2개, 게시글 5개, 댓글 5개(루트 2·답글 3), A→B 관계 1개입니다. 브라우저 대기 조건을 고쳐 재실행할 때는 동일 표식의 기존 글·답글을 찾아 재사용했습니다. 추가 계정·프로필 변경·SQL 변경·대량 자료 생성·삭제는 없습니다.

실제 브라우저 스크립트·진행 상태·로컬 화면은 Git 제외 경로에 보존했습니다. 로컬 화면의 사용자명·설정에서 유래한 표식은 후속 캡처에서 가렸고, 픽셀을 텍스트 검사로 정리할 수 없으므로 모든 이미지 첨부는 제출용 대화 기록에서 제거 표시만 남깁니다. 원본은 비공개 원시 기록에 보존합니다. 실제 공개 배포 화면이 아니므로 저장소에 배포 증거 이미지로 추가하지 않았습니다.

구성 근거: [Playwright Library](https://playwright.dev/docs/library), [checkout 7.0.1](https://github.com/actions/checkout/commit/3d3c42e5aac5ba805825da76410c181273ba90b1), [setup-node 7.0.0](https://github.com/actions/setup-node/commit/820762786026740c76f36085b0efc47a31fe5020).

### main 반영·GitHub CI 실행 (2026-09-17, 10:44~10:45 UTC)

사용자는 현재 작업의 commit·push와 CI 확인만 요청했으며 P4 전에 별도 교차 검토·디자인 수정을 진행한다고 명시했습니다. 이 단계에서는 앱 기능·디자인·SQL·계정 설정·배포를 변경하지 않았습니다. 라이선스 파일 2개의 줄 끝 공백과 마지막 빈 줄만 정리했고 문구는 보존했습니다.

| 검사            | 실제 실행 결과                                                                                                                                                                                                                                                                                                                                                      |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 로컬 검사       | `npm run check`에서 Prettier·ESLint·TypeScript와 8개 파일의 89개 단위 검사가 통과했습니다.                                                                                                                                                                                                                                                                          |
| Git·비밀값 검사 | 원격 `main`과 기존 HEAD 일치를 확인한 뒤 커밋했습니다. 후보 80개·실제 스테이징 77개 파일에서 런타임 설정값·토큰·내부 빌드 키 패턴을 검사해 통과했습니다. 실제 환경 파일·인증 상태·원시 AI 기록은 제외됐습니다. 라이선스 공백 정리 후 `git diff --cached --check`도 통과했습니다.                                                                                    |
| main push       | `git push origin main`으로 `7239402..ee0c460` 반영을 확인했습니다. 대상은 공개 저장소 `kimkangmin0914/8around-sns`입니다. 강제 push는 사용하지 않았습니다.                                                                                                                                                                                                          |
| GitHub CI       | [실행 35212085700](https://github.com/kimkangmin0914/8around-sns/actions/runs/35212085700)은 `ee0c4600d55ccc26d9693f8c605b1997d172c868`의 push로 시작했습니다. GitHub 러너에서 `npm ci`, `npm run check`, `npm run build`가 각각 성공했고 전체 결과도 `success`입니다. check 작업은 10:44:54~10:45:49 UTC에 실행됐습니다. 실제 DB 검사는 이 CI에 포함하지 않습니다. |

샌드박스의 Git 메타데이터 쓰기·외부 연결·하위 프로세스 제한은 승인된 재실행으로 해결했습니다. CI 검사는 기존 규칙을 유지해 통과했으며 공개 앱 배포는 실행하지 않았습니다.

### P3.5 교차 검토 수정·디자인 적용 (2026-09-17 UTC)

기준 HEAD는 `b2f28befbf9c31e486191390b8e6c065a395f861`입니다. 시작 시 `.agents/`와 `CODEX_P3_5.md`만 미추적 상태였고 기존 앱 수정은 없었습니다. 아래 결과는 현재 로컬 작업분이며 새로운 commit·push·공개 배포를 수행하지 않았습니다. 검토한 변경 45개 파일을 Git 스테이징에 준비했고 실제 스테이징 비밀값 검사도 통과했습니다. P3의 원격 CI 성공을 이 변경의 CI 성공으로 표시하지 않습니다.

**변경 범위.** `scripts/export-session.mjs`, `scripts/export-redaction.mjs`, export·프로필·피드 회귀 검사; `DESIGN.md`, 최소 AGENTS 안내와 기존 계획의 시각 기준; layout·globals.css·기존 페이지·작성자/댓글/팔로우/세션 컴포넌트; 세 vendor 스킬과 MIT 고지, `.prettierignore`, README·tool·이 문서·실제 export입니다. `CODEX_P3_5.md`는 사용자 정정인 CLI 2.0.0 및 포맷만 맞췄습니다. Server Actions·조회 함수·validation·Supabase SSR/proxy·migration·앱 의존성과 잠금 파일·과제 원문은 변경하지 않았습니다.

**재현 전후.**

| 대상                        | 수정 전 증거                                                                                                                                                                                                                                                                                                | 수정 후 결과                                                                                                                                                                                                                                                 |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| export escaped 값           | 임시 폴더의 가짜 비밀번호에 따옴표·역슬래시·개행을 넣었습니다. 기존 도구 arguments를 JSON.parse하면 원래 가짜 값이 복원되어 검사 실패.                                                                                                                                                                      | 일반 문자열·객체/배열·완전한 JSON·코드 속 JSON literal·이스케이프 조각을 지원 범위 안에서 가립니다. 0~6단계 회귀, 짧은 값, Unicode escape, 이미지·키 가림을 검사했습니다.                                                                                    |
| export 덮어쓰기             | 다른 가짜 세션을 두 번 내보낸 뒤 파일 수가 1개라 검사 실패.                                                                                                                                                                                                                                                 | 세션 SHA256과 실제 읽은 원본 SHA256의 파일명으로 두 세션이 공존합니다. 같은 원본은 idempotent 확인, 변경된 원본은 추가형 스냅샷. 충돌·symlink는 쓰기 거절. 기존 공개 스냅샷의 원본 hash 일치 사본 1개를 확인한 뒤 둘 다 보존했습니다.                        |
| 프로필 빈 다음 페이지       | 세 탭 모두 total=1/offset=20에서 ‘아직 … 없습니다’로 전체 빈 목록처럼 표시해 3개 검사 실패.                                                                                                                                                                                                                 | offset=0/0개, total=1/offset20, total=21/offset20의 마지막 1개, 조회 오류를 모의 응답으로 검사해 12개 통과. 실제 양수 관계의 offset20도 처음으로 링크와 다음 페이지 안내를 확인했습니다.                                                                     |
| 답글 취소 포커스            | 기존 실제 댓글에서 열기→입력→취소 후 같은 버튼이 activeElement가 아니었습니다.                                                                                                                                                                                                                              | ref callback으로 취소 후 그 부모 버튼에만 복귀. 초기 렌더 포커스 탈취 없음, Tab→Enter→입력→Tab→취소→같은 버튼→재열기와 Shift+Tab을 실제 Chromium에서 확인. 초안·pending 취소 금지를 유지했습니다.                                                            |
| SessionBoundary 조건부 위험 | 첫 missing-identity 시도는 차단 진입을 입증하지 못해 재현 성공으로 세지 않았습니다. 후속 통제 검사에서 A의 실제 쿠키를 유지하고 클라이언트 신원 응답만 일시적으로 다른 사용자로 바꾸고 RSC 도착을 지연했습니다. changed 차단 진입을 관찰한 뒤 실제 서버의 동일 A 응답을 받았지만 작성창은 0개로 남았습니다. | 검증된 서버 렌더마다 새 generation을 전달하고 차단된 세대만 숨깁니다. 같은 key·같은 A의 새 서버 세대에서 작성창 1개로 복귀했습니다. 클라이언트 신원 이벤트만으로 차단을 풀지 않습니다. 정상 재조회는 초안을 유지하며 A→B/A→guest는 이전 작성창을 해제합니다. |
| 팔로우 포커스·안내          | 실제 해제·재팔로우 모두 refresh 후 focus=false, 성공 live region 빈 값. 관계 상태를 key에 넣어 버튼이 재마운트됐습니다.                                                                                                                                                                                     | 대상 ID key를 유지하고 서버 관계 변화만 동기화합니다. 사용자가 다른 곳으로 포커스를 옮기지 않은 경우 완료 후 버튼으로 복귀하며, 두 동작 모두 focus=true와 실제 성공 문구가 유지됐습니다.                                                                     |

export는 JSON 인코딩 깊이 8·객체 깊이 32·문자열 512 KiB·원본 64 MiB 등의 경계가 있습니다. 경계 초과는 제외 표식 또는 공개 출력 중단이며 원본 바이트는 비공개에 남깁니다. 테스트가 임의 인코딩 전체를 보장하지 않습니다. 실제 환경값은 테스트 fixture에 쓰지 않았고 런타임에서만 비교했습니다. 기존 공개 기록 재검사에서 실제 설정값·토큰의 추가 가림 대상은 발견되지 않았습니다. 새 형식으로 실제 이벤트 552개를 내보냈고 가림 15개·경계 초과 제외 0개였습니다. 기존 스냅샷과 새 파일을 포함한 공개 후보 101개를 원문·중첩 이스케이프·토큰·첨부 데이터 기준으로 값 출력 없이 재검사해 통과했습니다. 새로운 실제 자격 증명 노출은 발견하지 않아 교체·공개 이력 정리를 수행하지 않았습니다. 가짜 이미지 URL도 공개 대화의 첨부 데이터에서는 제외합니다.

**OMD 로드·적용.** 설치된 `/home/codespace/.npm/_npx/766f9d15b3bd5af7/node_modules/oh-my-design-cli`의 CLI `--version`은 2.0.0, `install-skills --help`에서 선택 세 스킬·codex·skills-only·dir·lang 옵션을 확인했습니다. 사용자 정정에 따라 2.0.1 설치는 시도하지 않았습니다. 실제 경로는 `/workspaces/8around-sns/.agents/skills/omd-apply/SKILL.md`, `omd-feel/SKILL.md`, `omd-slop-audit/SKILL.md`이며 frontmatter는 각각 `omd:apply`, `omd:feel`, `omd:slop-audit`입니다. 셋 모두 현재 Codex 스킬 목록에 노출되고 문서를 직접 로드했습니다. 전용 스킬 호출 API가 제공되지 않아 문서 직접 적용 fallback으로 처리했으며 런타임 스킬 호출·전문 역할 실행 완료로 기록하지 않습니다. 설치 버전과 파일 대응을 비교했으며 apply/slop의 차이는 채널용 이름·설치 표식·빈 줄이고 feel 본문은 같았습니다. GitHub 지정 commit과 npm 배포물의 동일성까지 확인한 것은 아닙니다.

`omd:apply`의 브랜드 계약 적용·동작 보존, `omd:feel`의 실제 대비·포커스·타깃·동작 감소, `omd:slop-audit`의 동봉 rule catalog와 같은 화면 전후 비교만 적용했습니다. 사용자 문서가 금지한 별도 시안 앱·proof-policy·hooks·하위 역할·추가 설치는 하지 않았습니다. 고정 GitHub 참고 URL은 웹 도구에서 cache miss였고 OMD karrot 카탈로그·SEED 진입점/llms.txt·W3C 설명은 조회했습니다. 콘텐츠 우선·읽기 위계·명확한 상태만 채택했고 타사 브랜드 자산·거래 UI·마케팅 수치는 가져오지 않았습니다. 토큰은 `DESIGN.md`의 ‘사이’ 결정입니다.

**화면 근거와 비교.** 기존 실제 점검 데이터(비교 시점 글 5개·댓글 5개)를 바꾸지 않은 상태에서 `/`의 A/guest, 기존 `/posts/[id]?thread=[기존 부모]`, `/u/[A]`, `/people`, `/login`, `/signup`을 각각 390×1000·1440×1000으로 전후 14쌍 캡처했습니다. 앞의 네 로그인 화면은 A, 인증/게스트는 비로그인입니다. 각 쌍의 실제 본문과 폼 수가 같음을 대조했습니다. `.ai-raw/p35-before-*.png`, `p35-after-*.png`에 비공개로 보관하고 설정 유래 사용자명·표식은 가렸습니다. 후속 저장 점검으로 추가된 자료를 앞선 전후 이미지의 데이터로 혼동하지 않습니다. 이미지 첨부는 공개 export에서 제외합니다.

| 구분                                | route·코드 근거                                                                                                            | 판단과 변경                                                                                                                                                           |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WARN · SLOP, COPY-01 / LAYOUT-07    | 변경 전 게스트 `/`의 감성 제목·소개, 로그인·사람 목록의 반복 eyebrow. 현재 `src/app/page.tsx:34`, `src/app/layout.tsx:33`. | 문구 묶음이 실제 목록보다 먼저 자리했습니다. 전체 글 제목과 로그인 필요 행동만 남기고 상단 탐색으로 이동했습니다. 특정 색/카드 하나를 AI 출처로 판정한 것이 아닙니다. |
| 프로젝트 계약·정보 위계             | `src/components/post-row.tsx:14`, `src/app/globals.css:297` 및 댓글·person 행.                                             | 카드형 작성 영역과 넓은 좌측 탐색을 평평한 작성 영역·40px 이니셜·본문 열로 대체. 상세와 답글은 같은 정렬과 얇은 연결선. 본문/실제 글/링크 경로를 보존했습니다.        |
| BLOCK → 해결 · QUALITY, STATE-02    | `src/app/u/[username]/page.tsx:131`, `:150`.                                                                               | 다음 빈 페이지와 전체 빈 목록을 구분하고 기존 처음으로 링크를 유지했습니다.                                                                                           |
| BLOCK → 해결 · QUALITY, focus/state | `src/components/comment-composer.tsx:41`, `src/components/follow-button.tsx:26`, `src/components/session-boundary.tsx:63`. | 실제 focus·성공 안내·조건부 차단 문제를 각각 재현 후 고쳤습니다. 취향 점수와 합산하지 않았습니다.                                                                     |
| FYI · PREFERENCE                    | `DESIGN.md`의 먹색 주 행동·주황 활성·그림자 없는 목록.                                                                     | 사용자가 확정한 프로젝트 선택입니다. OMD 휴리스틱의 50ms 배수·다층 그림자·스케일 애니메이션을 점수 때문에 도입하지 않았습니다.                                        |

**실제 검사.**

- `npm run check`: P3.5 앱 변경 후 113개 통과. 이후 export 경계·피드 상태 검사를 포함한 최종 `npm run check`에서 11개 파일·120개 검사가 모두 통과했습니다(13:06 UTC). 기존 검사 규칙은 끄지 않았습니다. vendor `reflow-artifact.mjs:1258` 미사용 함수 warning 1개는 남습니다(앱 오류 0). 전체 `git diff --cached --check`는 vendor feel의 `provenance.md:3,4`, `reference.md:3`에 있는 Markdown 두 칸 줄바꿈 3곳을 공백 경고로 보고했습니다. 원본은 보존했으며 vendor를 제외한 앱·문서 diff 검사는 별도로 통과했습니다.
- `npm run build`: webpack 빌드와 TypeScript 통과, 기존 8개 동적 route와 Proxy 확인. 팔로우 수정·이니셜 변경을 반영한 재빌드도 통과했습니다. 새 의존성·새 route·SQL/Auth 설정 변경 없음.
- 실제 Chromium: `/`, 기존 상세, 사람 목록, B 프로필, B followers, A following, 로그인, 가입의 8개 route를 320/390/768/1440px에서 확인했습니다. 페이지·주요 제어 가로 넘침 0, 측정한 button/nav/profile-tab의 최소 타깃 44px. 640px viewport에 실제 CSS zoom=2를 적용한 200% 상당 reflow도 8개 route에서 넘침이 없었습니다. 브라우저 메뉴의 네이티브 확대나 OS 입력기를 실행한 증거와 구분합니다.
- 실제 computed style: 일반 텍스트 최소 대비 4.7876:1, placeholder 6.0498:1, 입력 경계 3.2558:1, 텍스트 opacity 1. 토큰 조합 별 계산에서 오류·성공·보조·링크도 지정 표면에 4.5:1 이상이고 입력 경계는 3:1 이상입니다. 동작 감소 시 transition=0s, 키보드 skip link 표시/본문 이동·Shift+Tab·답글 포커스를 확인했습니다. 별도 마지막 확인에서 textarea를 실제로 focus한 뒤 주황 `rgb(194, 65, 12)`·2px solid·4px offset·focus-visible과 viewport 안쪽 표시를 측정했습니다.
- 실제 실패·세션 회귀: DB 도착 전 POST abort 시 글·답글 초안과 부모·재조회 수단 보존, 팔로우는 확정 상태 유지. 처리 중 중복 submit 차단과 자동 재전송 없음. A→B·A→guest·신원 조회 통신 실패·A의 늦은 응답과 실제 Auth refresh-token 갱신을 확인했습니다. 세션/요청 값은 출력하지 않았습니다.
- 실제 DB 도착 후 응답 유실: A의 UI 저장 요청을 서버까지 전송하고, 정상 서버 응답과 DB의 실제 1행을 먼저 확인한 다음 브라우저 응답만 중단했습니다. 미확정 안내·동일 초안·한 요청/한 행·재조회 후 초안 보존을 확인했습니다. 별도 B가 그 글을 읽고 댓글을 UI로 저장했으며 A가 그 부모에 답글을 저장했습니다. B의 독립 조회에서 정확한 본문·작성자·부모를 확인했습니다.
- 이번 추가량은 `[점검 P3.5 <실행 식별자>]` 글 1개·최상위 댓글 1개·답글 1개입니다. 기존 자료를 재사용한 별도 flow 검사는 추가 작성하지 않았습니다. 팔로우는 승인된 A 본인의 A→B를 해제·복원해 최종 1개를 유지했습니다. 누계는 계정/프로필 2개, 글 6개, 댓글 7개(루트 3·답글 4)입니다. 초기화·삭제·부하·추가 계정·SQL 변경은 없습니다. P3의 `test:access` 결과는 별도 기존 증거이며 이번 UI 수정에서 다시 실행하지 않았습니다.

**미검증·P4 잔여.** OS 한국어 IME 조합, 브라우저 메뉴의 네이티브 200% 확대, 새 외부 브라우저 회원가입 및 초기 프로필 완료는 미검증입니다. 기존 A/B는 완료된 프로필이라 onboarding 브라우저 체험을 위해 계정을 새로 만들거나 프로필을 지우지 않았습니다. 긴 표시 이름·소개, 전체 빈 피드/조회 오류, 21번째 pagination은 모의 서버 응답 검사이며 실제 DB 대량 자료 검사가 아닙니다. P3.5의 원격 CI·공개 앱 배포는 아직 없으며, 공개 URL·deploy ID·새 외부 가입부터 전체 체험·native export 대안 형식 수용 여부·최종 내보내기·제출이 P4에 남습니다.

### P3.5 서비스명·화면 안내 정제 (2026-09-17 UTC)

과제 원문의 `8around`는 전형·제출 대상 명칭이며 SNS 서비스명을 강제하지 않습니다. 헤더·접근성 이름·메타데이터는 사용자가 지정한 ‘단풍’으로 변경했습니다. 가입 안내와 비밀번호·댓글·팔로우의 기술 표현은 화면에서만 풀어 썼고 글·댓글 카운터에는 ‘자’를 붙였습니다. 서버가 반환한 검증 메시지는 표시 단계에서만 바꾸며 결과 상태·검증 조건·초안을 유지합니다. `src/actions`, `src/lib`, SQL, 의존성, 과제 원문은 HEAD와 동일함을 diff로 확인했습니다. 테스트·기존 브라우저 스크립트에는 서비스명 단언이 없었고 ‘사이’라는 임의 입력 데이터는 보존했습니다.

`npm run check`는 11개 파일의 120개 검사와 포맷·타입·lint를 통과했습니다. 기존 vendor ESLint warning 1개는 그대로입니다. `npm run build`도 8개 동적 route와 Proxy로 성공했습니다. 실제 Chromium에서 같은 비로그인 상태의 `/login`, `/signup`을 320·390·1440px로 전후 비교했습니다. 헤더·title·접근성 이름·가입 힌트를 확인했고, 두 폼의 한글 25자·영문 73자 거절 및 가입 폼의 7자 거절, 입력 보존, 가로 넘침 없음이 전후 동일했습니다. 길이 초과 오류에서 ‘UTF-8·바이트’가 사라지고 한글·이모지 포함 시 허용 길이가 짧아짐을 안내합니다. 브라우저 검사 중 쓰기 요청은 0개였으며 캡처와 검사 스크립트는 `.ai-raw/copy-*`, `.ai-raw/check-copy.mjs`에 보관했습니다.

설정 오류·저장 권한 오류 등 서버 실패 분기의 새 표시 문구는 코드 검토 범위이며 이번에 실제 장애를 만들지는 않았습니다. DB 쓰기 검사·원격 CI는 재실행하지 않았습니다. 기존 P3.5 스테이징을 보존하고 이번 문구 변경은 미스테이징 상태로 두었습니다. commit·push·P4 배포는 사용자 지시에 따라 대기합니다.

## 3. 차단·미검증

- DB에 적용한 migration: `202609170001_initial.sql` (소유자가 과제 전용 프로젝트에 적용 완료를 확인). 익명 로그인 비활성도 소유자 확인을 받았습니다. 후속 공개 조회에서 네 테이블 및 게시글→작성자 관계는 HTTP 200으로 확인했습니다. 실제 P1/P2 쓰기·거절 정책의 확인은 위 별도 증거에 기록했습니다.
- 실제 신규 가입·로그인·프로필·A 작성/B 조회·쿠키를 복원한 새 HTTP 요청·로그아웃 쓰기 거절: 위 P1 점검에서 통과했습니다. P2 댓글·팔로우는 위 실제 점검에서 추가로 통과했습니다. `npm run test:access`는 P3에서 구현·실행해 통과했습니다. 위 P3 표에 실제 DB 증거를 구분했습니다.
- 실제 Chromium의 390px/1440px·기본 키보드 이동·초안 보존·계정 전환·쿠키 갱신은 위 범위에서 통과했습니다. OS 한글 입력기 조합, 긴 표시 이름의 실제 계정, 초기 전체 빈 피드는 미검증입니다. 기존 프로필·DB를 변경하거나 비우지 않았으며, 글 없는 프로필·빈 다음 페이지는 확인했습니다. 신규 가입 자체의 브라우저·외부 방문자 검증은 P4에 남습니다.
- 공개 URL·Netlify 사이트·deploy ID: 없음. `main` push와 원격 GitHub CI는 위 기록대로 완료했습니다. 공개 앱 배포·계정 설정 변경은 수행하지 않았습니다.
- `exports/`에는 기존 `codex-session.jsonl`과 세션/원본 hash별 새 스냅샷을 보존합니다. 모두 실제 rollout의 사용자·응답·도구 이벤트입니다. CLI의 전용 transcript export 명령은 확인하지 못했습니다. 대안 형식의 과제 요건 충족과 이 응답 이후 최종 스냅샷은 제출 전 확인사항입니다. 내부 추론·시스템 지침·중복 이벤트는 제외했으며 원본은 별도 보존합니다.
- ESLint 9.39.5의 지원 종료 경고와 설치 시 `unrs-resolver` 선택 postinstall 미승인 경고가 있습니다. 현재 check·build는 실행 결과로 판단하며 검사 규칙을 끄지 않았습니다.

## 4. 다음 작업

2026-09-17 10:06 UTC 재개 시 백그라운드 `npm run start` 프로세스가 계속 실행 중임을 확인했고 `http://localhost:3000`은 HTTP 200이었습니다. 요청을 중단하는 브라우저 검사 과정에서 stream 종료 로그가 남았으나 상태 조회는 정상입니다. 확인용 로컬 서버는 유지합니다. 점검용 브라우저는 종료됐습니다.

P3.5 교차 검토 수정·디자인 구현과 로컬 검증을 위에 기록했습니다. 새로운 commit·push와 P4 배포는 수행하지 않았습니다. 이후 배포 설정·외부 전체 흐름 확인은 README의 소유자 설정 절차에 따라 수행합니다.

추가 SQL 적용·점검 계정 생성은 필요하지 않습니다. 공개 앱 배포·과제 이메일 제출은 아직 수행하지 않았습니다. 로컬 확인을 마치면 소유자가 Codespace를 중지합니다.
