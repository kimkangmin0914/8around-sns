# 작업 규칙

- `docs/assignment.md`와 `IMPLEMENTATION_PLAN.md`를 기준으로 구현한다. 충돌하면 과제 원문을 우선한다.
- 현재 단계의 코드·검사만 읽고 고친다. 범위와 기술 스택을 임의로 확장하지 않는다.
- P0부터 진행하며 작은 변경마다 검증한다. 반복적인 계획 문서는 생성하지 않는다.
- 기존 파일과 작업 내용을 먼저 확인한다. 초기화 도구로 기존 소스를 덮어쓰지 않는다.
- 저장소 내부의 승인된 변경은 계속 진행한다. 비용·계정 변경·SQL 적용·공개 배포에는 대상과 행동을 명시한다.
- 익명 쓰기·작성자 사칭·잘못된 댓글 연결을 거절한다. RLS나 검사를 꺼서 통과시키지 않는다.
- DB reset, 데이터 일괄 삭제, 부하 검사는 하지 않는다. 다른 서비스의 DB를 사용하지 않는다.
- 비밀번호·토큰·관리 키를 출력하거나 커밋하지 않는다. `.codex/auth.json`을 읽거나 제출하지 않는다.
- 필요한 테스트 설정은 런타임이 읽게 한다. 설정 파일의 실제 값을 AI 응답·로그에 복사하지 않는다.
- 가짜 계정 선택이나 브라우저 메모리를 실제 로그인·저장으로 대체하지 않는다.
- 사용자 입력·통신 실패·성공 결과를 구분한다. 실패하면 초안을 보존한다.
- 주요 변경 후 `npm run check`, 배포 전 `npm run build`를 실제 실행한다. 명령이 없으면 계획대로 먼저 구현한다.
- `npm run test:access`는 명시적으로 확인한 과제 프로젝트·점검 계정에서만 실행한다. 점검 자료만 추가하며 초기화하지 않는다.
- 단위 검사·DB 검사·브라우저 확인·배포 확인을 서로 다른 증거로 기록한다.
- 실제 AI 기록을 보관한다. 내보내기 전 비밀을 정리하고 사후 생성 요약으로 원본 대화를 대체하지 않는다.
- 보고는 변경 파일, 실제 검사 결과, 차단·미검증, 다음 작업만 정리한다.
