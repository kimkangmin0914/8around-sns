# P4 — 단풍 공개 배포와 제출 마감

기준 저장소: `kimkangmin0914/8around-sns`.
검토한 기준 commit: `2539ed35691885ca17f46c7fc17296bb6ff8ceb5`.
우선순위: `docs/assignment.md` → `AGENTS.md`의 권한·데이터 보호 규칙 → 이 문서의 P4 작업 → 기존 기능 명세와 `DESIGN.md`.

현재 서비스명은 **단풍**이다. 추가 승인된 리브랜딩과 문구 정제 결과를 보존한다. 기존 명세의 ‘사이’ 표현 때문에 화면을 되돌리지 않는다. 저장소 이름·테스트 입력·역사적 대화 속 단어를 일괄 치환하지 않는다.

## 1. 작업 범위와 종료 조건

P3.5의 기능·디자인을 유지한 채 다음을 마친다.

- 실제 접속 가능한 Netlify 정식 URL에서 신규 가입 → 로그인 → 게시글 → 댓글/답글 → 팔로우와 두 목록을 확인한다.
- 프로젝트 루트의 **`README.md`**에 §8의 완성 문안을 적용한다. `read.md` 등 별도의 입문 문서를 만들어 내용을 분산하지 않는다.
- `tool.md`, `docs/verification.md`, `exports/README.md`, 실제 대화 파일과 라이선스를 현재 작업에 맞춘다.
- Public 저장소, 정식 배포, 검증 대상 코드가 같은 기능 상태를 가리키는지 확인한다.
- 제출 이메일에는 GitHub와 배포 URL을 모두 넣는다. 초안 작성과 실제 발송을 구분한다.

범위 밖: 새 기능, 다른 플랫폼으로의 이전, DB 분리·초기화, 대규모 리팩터링, OMD 재설치·업데이트, 디자인 재선정, 근거 없는 의존성 전체 업데이트.

제품·문서의 잘못을 숨기기 위해 RLS·검사·실패 상태를 제거하지 않는다. 배포를 막는 결함을 실제로 확인하면 해당 부분만 최소 수정하고 재검증한다. 승인되지 않은 비용·계정 정책 변경·SQL 적용·새 계정 생성·push·배포·메일 발송은 수행하지 않는다. 이미 승인받은 같은 범위를 반복해서 묻지 않는다.

## 2. 시작 확인

1. `git status --short`, 현재 HEAD, 원격 `main`과의 차이를 확인한다. 기존 미커밋 변경을 보존한다. 기준 이후 코드가 바뀌었다면 그 차이부터 읽는다.
2. `docs/assignment.md`, `AGENTS.md`, 현재 `IMPLEMENTATION_PLAN.md`, `DESIGN.md`, `package.json`, `netlify.toml`, `.env.example`, 검증 기록의 최신 부분을 읽는다. 과거 설계 문서를 모두 다시 읽거나 재작성하지 않는다.
3. 기존 실행 환경과 필요한 값의 존재만 확인한다. `.env`·점검 계정·토큰의 실제 값은 AI 응답과 로그에 출력하지 않는다. 배포를 위해 `.codex/auth.json`을 읽지 않는다.
4. 실행 결과를 받을 수 있는 환경에서 `npm run check`, `npm run build`를 순서대로 실행한다. 빌드를 동시에 두 개 시작하지 않는다. 다른 작업이 진행 중이면 기존 프로세스와 산출물을 먼저 확인한다.
5. 기준 commit의 GitHub CI 실행은 `35228741200`이며 120개 테스트와 빌드가 통과했다. 이는 그 commit의 근거다. 이후 변경을 자동으로 통과 처리하지 않는다. `.invalid` 연결값을 쓴 CI 성공을 실제 DB·배포 성공으로 바꾸지 않는다.
6. 기존 기록의 ‘P3.5는 로컬 변경’·‘원격 CI 없음’·‘commit 대기’는 당시 상태로 보존한다. 현재 요약은 실제 HEAD와 CI로 갱신한다.

기능·세션·데이터 보호를 깨뜨리는 실패가 나오면 공개 배포를 멈추고 원인과 최소 수정 범위를 보고한다. ESLint 지원 종료 및 vendor 미사용 함수 등 기존 경고는 오류와 구분한다. 릴리스 직전에 광범위한 업그레이드로 숨기지 않는다.

## 3. 문서 준비

### 3.1 문서별 역할

| 파일                     | 결과                                                                          |
| ------------------------ | ----------------------------------------------------------------------------- |
| `README.md`              | 평가자가 앱을 사용하고 실행 구조와 선택 이유를 이해하는 첫 문서. §8 사용.     |
| `tool.md`                | 실제 사용한 AI·스킬·검사 도구와 사용 범위. §9 사용.                           |
| `docs/verification.md`   | 현재 배포·코드·검사 결과 요약, 실제 접근 검사 설정, 상세 과거 기록. §10 사용. |
| `exports/README.md`      | 어떤 대화 파일을 먼저 읽을지 알려 주는 짧은 목차. §11 사용.                   |
| `THIRD_PARTY_NOTICES.md` | 기존 출처·라이선스 유지. 사용하지 않은 패키지나 브랜드 자산을 추가하지 않음.  |

README의 문장은 §8을 기본으로 적용한다. 배포 URL 및 실제 코드·검증과 어긋나는 사실만 근거를 확인하여 바꾼다. 다시 홍보 문구나 기술어 목록으로 재작성하지 않는다. ‘핵심 가치’, ‘견고한 아키텍처’, ‘혁신적’, ‘완벽한’ 등의 평가 표현과 허구의 제작 경험을 추가하지 않는다.

필수 용어는 필요해지는 문장에서 풀어 쓴다. 예: ‘하네스 → 코드를 바꿀 때 같은 규칙과 검사로 확인하는 작업 환경’, ‘RLS → 사용자별 데이터 접근 규칙’. 결과와 이유를 연결하고, README에서 긴 실행 로그·해시·시간표를 반복하지 않는다. 자세한 근거는 검증 기록에 연결한다.

§8의 `{{DEPLOY_URL}}`은 실제 생성·확인한 **정식 HTTPS 주소**로만 치환한다. 아직 주소가 없다면 배포되지 않았다는 임시 문구를 두고 작업 중으로 남긴다. 존재하지 않는 주소를 만들거나 Codespaces 주소를 넣지 않는다. 최종 제출본에 배포 URL 자리표시자를 남기지 않는다.

### 3.2 리브랜딩·문구 대조

- 현재 화면·메타데이터·접근성 이름·README·DESIGN의 서비스명은 ‘단풍’으로 맞춘다.
- 원문 속 8around, 과거 대화·시험 데이터의 ‘사이’, 내부 패키지 이름 `sai-sns`는 서비스명 표시와 구분한다. 출시를 위해 무관한 식별자를 일괄 변경하지 않는다.
- 가입 정책, 프로필 수정 불가, 길이 제한, 미확정 안내는 쉬운 문장으로 바꾸더라도 의미를 줄이지 않는다.
- 실제 사용한 OMD CLI는 2.0.0이다. 2.0.1을 사용했다고 기록하거나 P4에서 다시 설치하지 않는다. 세 스킬은 지침을 직접 읽어 적용한 것으로 기록하며, 전용 호출·하위 전문가 실행을 꾸미지 않는다.

## 4. Netlify 정식 배포

### 4.1 외부 작업 승인과 설정 확인

기존 Netlify 사이트가 있으면 재사용한다. 없다면 소유자의 승인과 기존 Free 한도 안에서 GitHub 저장소를 연결한다. 사이트 생성·배포·필요한 설정 변경의 대상과 행동을 한 번에 알린다. 결제·유료 전환·도메인 구입을 하지 않는다.

유지할 구성:

| 설정              | 값                                  |
| ----------------- | ----------------------------------- |
| Repository        | `kimkangmin0914/8around-sns`        |
| Production branch | `main`                              |
| Base directory    | 저장소 루트                         |
| Build command     | `npm run check && npm run build`    |
| Publish directory | `.next`                             |
| Node              | 24                                  |
| Framework         | Next.js, Netlify 기본 OpenNext 지원 |

기존 `netlify.toml`과 UI 설정의 충돌을 확인한다. 정적 `out` 배포·HTML 드래그앤드롭·별도 SPA fallback으로 바꾸지 않는다. Server Actions, 동적 화면과 쿠키가 실제로 동작해야 한다.

환경변수는 Netlify의 보안 설정 화면 또는 승인된 비밀 전달 경로로 등록한다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=<확인한 과제 프로젝트 URL>
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<같은 프로젝트의 publishable key>
```

Builds와 Functions에서 사용할 수 있게 설정한다. CI의 `.invalid` 주소·가짜 키를 복사하지 않는다. 실제 `.env.local`, 점검 계정, 관리자 키, Codex 자격 증명을 배포에 올리지 않는다. `NEXT_PUBLIC_` 값은 빌드에 반영되므로 수정했다면 새 빌드에서 확인한다.

### 4.2 기존 Supabase 확인

- 과제 전용 프로젝트가 활성 상태인지, 앱과 같은 프로젝트인지 확인한다.
- 초기 migration `202609170001_initial.sql`은 이미 적용된 프로젝트에 다시 실행하지 않는다. RLS·열 권한·제약은 유지한다.
- 신규 가입과 Email 로그인 허용, Confirm Email 비활성, 익명 로그인 비활성을 확인한다. 기본 Auth 요청 제한을 완화하지 않는다.
- Site URL은 승인 후 정식 배포 주소와 맞춘다. 지금 사용하지 않는 OAuth·메일 콜백이나 광범위한 redirect wildcard를 추가하지 않는다.
- 이메일 확인을 다시 켜거나 SMTP를 추가하는 정책 변경은 P4의 기본 작업이 아니다.

### 4.3 배포 결과 확인

배포 로그에서 실제 commit, 설치·check·build 결과, 배포 ID, 정식 URL을 기록한다. 주소가 열리는 것만으로 성공 처리하지 않는다. 비로그인 브라우저에서 CSS·폰트·스크립트와 `/`, `/people`, `/login`, `/signup`을 확인하고, 실제 글·프로필 상세의 새로고침이 동작하는지 본다.

인증·쓰기의 출처 검사나 쿠키 문제가 생기면 정식 host와 제공자 로그로 원인을 좁힌다. wildcard 허용이나 보안 기능 해제로 통과시키지 않는다. 프로젝트 설정이 필요하면 정확한 최소 변경만 요청한다.

## 5. 공개 주소의 전체 체험

### 5.1 데이터 변경 한도

현재 검증 계정을 우선 재사용하되 신규 가입 검증은 기존 계정 로그인으로 대신하지 않는다. 기본 추가 요청 범위는 **새 시험 계정 C와 프로필 1개, 게시글 1개, 댓글 1개, 답글 1개**다. 파트너는 기존 점검 계정 B를 사용한다. 자격 증명이 없으면 두 번째 신규 계정의 필요성을 별도로 알린다. 실제 사람의 주소를 임의 생성하지 않고 소유자가 허용한 시험 주소를 사용한다.

자료에는 `점검 P4`와 실행 식별자를 넣는다. C가 만든 C→B 관계의 생성·해제만 검사한다. 기존 A→B 등 다른 관계를 바꾸지 않는다. 수량·잔존 결과를 기록하며 초기화·대량 생성·삭제·부하 검사는 하지 않는다. UI가 삭제를 제공하지 않으므로 실패 후 재시도 전에 이전 저장 여부부터 확인한다.

### 5.2 두 개의 독립된 브라우저 문맥

| 순서 | 행동                                                          | 확인할 결과                                                     |
| ---- | ------------------------------------------------------------- | --------------------------------------------------------------- |
| 1    | 비로그인으로 정식 URL 방문                                    | 플랫폼 로그인·암호 보호 없이 공개 글·사람 목록을 읽음           |
| 2    | C를 앱의 가입 화면에서 새로 등록                              | 실제 계정 생성. 이메일 확인 대기 루프 없이 프로필 단계로 이동   |
| 3    | 사용자명 충돌 후 다른 이름으로 프로필 저장                    | 계정 재생성 없이 복구. 정상 저장 후 전체 글 화면                |
| 4    | C 로그아웃 → 앱 로그인 폼으로 재로그인 → 새로고침·페이지 이동 | 같은 C와 작성 폼 유지. 비밀번호·쿠키를 로그에 남기지 않음       |
| 5    | C가 게시글 작성                                               | 성공 이후 입력 초기화. 피드·상세·C 프로필에서 실제 같은 글 확인 |
| 6    | 별도 문맥 B가 해당 글 조회·댓글 작성                          | 동일한 본문·작성자 확인, 새 댓글이 실제로 저장됨                |
| 7    | C가 B의 댓글에 답글 작성                                      | 같은 글·부모에 연결됨. 펼치기·취소·재열기·포커스 정상           |
| 8    | C가 B 팔로우                                                  | C 팔로잉과 B 팔로워에 같은 관계·수 표시                         |
| 9    | C가 해제                                                      | 양쪽 목록·수에서 같은 관계가 없어짐                             |
| 10   | 로그아웃 후 쓰기 시도 및 공개 조회                            | 쓰기는 거절되고 공개 읽기는 유지                                |
| 11   | 개발 서버와 Codespace를 중지한 뒤 외부에서 다시 열기          | 제출 서비스가 개발 컴퓨터를 필요로 하지 않음                    |

B 브라우저의 화면까지 실제로 확인한다. B의 DB 직접 조회만을 외부 브라우저 체험 완료로 기록하지 않는다. 신규 가입은 HTTP 스크립트로 대신하지 않는다.

### 5.3 마감 전 확인할 작은 동작

- 390px·1440px에서 헤더, 글, 댓글, 프로필, 인증 화면을 확인한다. 320px와 긴 이름·URL도 가능한 기존 자료로 점검한다.
- OS 한국어 입력기에서 조합 중 Enter가 의도치 않은 제출로 이어지지 않는지 확인한다. 자동화만 했다면 실제 OS 입력기 통과로 적지 않는다.
- Tab/Shift+Tab/Enter, 답글 취소 후 같은 버튼 복귀, 팔로우 후 포커스·안내, 200% 브라우저 확대를 확인한다. CSS zoom 검사를 실제 브라우저 메뉴 확대와 구분한다.
- 글·답글의 초안 보존과 팔로우의 확정 상태를 유지한다. 저장 전 abort와 저장 후 응답 유실을 구분한다. P3.5의 기존 증거를 재사용하면 그때의 결과라고 적는다.
- 외부 배포에서 쿠키 갱신·재로그인·A/B 화면 분리를 확인한다. 사용자별 HTML과 Set-Cookie 응답을 공용 캐시에서 재사용하지 않는지 확인한다.
- `npm run test:access`가 필요하면 준비된 설정과 기존 승인 범위를 확인한 뒤 실행한다. 매번 자료가 추가된다는 점을 알리고 결과를 외부 UI 검사와 구분한다.

필수 흐름이 실패하면 제출 완료로 표시하지 않는다. 재현 위치·응답·허용된 최소 수정만 남기고 수정 후 관련 정상 흐름도 다시 확인한다.

## 6. 실제 기록·출처·최종 Git 정리

1. 작업하는 동안 실제 세션 원본을 보존한다. 마지막 기능·배포 점검 뒤 기존 `scripts/export-session.mjs`로 P4 대화를 내보낸다. 없는 대화나 도구 결과를 만들어 채우지 않는다.
2. 기존 세 파일과 새 파일 모두 공개 후보다. 원문·이스케이프·토큰·개인정보·이미지 첨부를 런타임에서 검사한다. 검사 범위와 제외 개수만 보고하고 값은 출력하지 않는다. 이번 감사의 가짜 값 검사 통과를 실제 공개 파일 전체의 안전성 보장으로 사용하지 않는다.
3. 새로운 파일을 만들었다는 이유로 기존 파일·원본을 무작정 지우지 않는다. 같은 세션의 누적 스냅샷 관계를 `exports/README.md`에 표시한다. 검사를 우회하려고 본래 질문·응답을 요약으로 바꾸지 않는다.
4. 설계·감사에 사용한 ChatGPT와 구현에 사용한 Codex를 `tool.md`에서 구분한다. 사용한 관련 대화의 실제 내보내기 자료가 없다면 소유자에게 그 파일을 요청하고 미확보 상태를 기록한다. 이 작업서를 실제 대화 export로 대신하지 않는다.
5. `filtered-codex-rollout`은 공식 CLI transcript export가 아니다. 과제의 export 방식으로 수용되는지 확인한 근거가 없으면 확인 필요로 남긴다. 문의할 경우 수신자는 원문의 `jhlee@8around.kr`이며, 외부 발송은 승인 후에만 한다. 미확인을 임의로 승인됨으로 바꾸지 않는다.
6. 실제 비밀 노출을 발견하면 공개 작업을 멈추고 종류·위치·조치 필요성만 알린다. 자격 증명 교체와 Git 이력 정리는 소유자 승인 경계다.
7. `.env*` 실제 파일, `.codex`, `.ai-raw`, 로그인 상태·브라우저 trace, 토큰이 담긴 스크린샷을 커밋하지 않는다. 정리된 앱 화면을 증거로 남길 때는 실제 배포 화면인지와 캡처 대상을 명시한다. 가짜 이미지로 대체하지 않는다.
8. 원문, 라이선스와 스킬 원본은 보존한다. 코드를 쓰지 않은 SEED 패키지나 전용 디자인 에이전트를 사용했다고 적지 않는다.
9. 커밋 전 실제 변경 파일을 명시적으로 선택한다. 원시 기록까지 들어갈 수 있는 무분별한 `git add .`, 강제 push, 기존 변경 폐기를 피한다. 문서의 링크와 코드블록도 확인한다.

최종 문서 변경까지 포함한 `check`·`build`와 GitHub CI를 확인한다. 파일 수·검사 수·경고는 실제 출력으로 갱신한다. 현재 원격과 로컬의 차이는 사실대로 남긴다.

## 7. 최종 규격과 제출

| 원문 항목        | 제출 직전 증거                                      |
| ---------------- | --------------------------------------------------- |
| 가입·로그인·세션 | 정식 URL의 새 계정 C와 로그인·새로고침 결과         |
| 게시글·피드      | C의 게시글을 B가 실제 화면에서 읽은 결과            |
| 댓글·계층 답글   | B 댓글에 C 답글이 같은 부모 아래 나타남             |
| 팔로우·두 목록   | 생성·해제 후 양쪽 목록·수 일치                      |
| 디자인·피드백    | 단풍 브랜드, 정돈된 화면, 오류·pending·focus 유지   |
| 하네스           | 실행 방법·규칙·check/build/접근 검사와 실제 결과    |
| README 여섯 내용 | §8의 여섯 제목과 실제 배포 URL, 관련 파일 링크      |
| tool.md          | 실제 도구·용도·버전·스킬 적용 방식                  |
| exports/         | 실제 대화, 개인정보 점검, 읽을 순서, 형식 확인 상태 |
| Public GitHub    | 로그아웃한 브라우저에서도 소스 접근 가능            |
| 공개 배포        | 개발 환경을 꺼도 정식 URL에서 전체 흐름 사용 가능   |
| 이메일           | GitHub·정식 배포 URL을 포함해 실제 발송             |

소스와 배포를 연결할 때 커밋 해시를 자기 문서에 무한히 다시 삽입하지 않는다. 문서에는 **검증한 앱 commit과 배포 ID**를 기록한다. 이후 문서·대화만 바뀐 commit이 있으면 앱·설정·잠금 파일·SQL이 같음을 diff로 확인해 관계를 적는다. 런타임 파일이 바뀌었다면 재빌드·재배포·관련 재검증을 한다.

정식 서비스는 이메일 발송 시점의 저장소와 같은 기능 상태여야 한다. 읽기 전용 최종 점검 이후 추가 작업이 생기면 새 실제 기록을 보존한다. 모든 최종 외부 쓰기는 이메일 발송 전에 끝내고, 승인 없는 발송 이후 변경을 하지 않는다.

원문 기한: **2026년 9월 17일 23:59:59**. 시간대는 원문에 명시되어 있지 않다. 실제 이메일 발송 시점이 제출 기준이며 push나 초안 저장은 제출이 아니다. 남은 시간·접수 여부를 추정하거나 기한을 임의로 변경하지 않는다.

이메일 초안:

```text
수신: jhlee@8around.kr
제목: [8around 과제 제출] {{APPLICANT_NAME}}

안녕하세요. {{APPLICANT_NAME}}입니다.
SNS 프로젝트 ‘단풍’을 제출합니다.

GitHub: https://github.com/kimkangmin0914/8around-sns
서비스: {{DEPLOY_URL}}

저장소의 README에 사용·실행 방법과 구현 결정을 정리했습니다.
사용 도구는 tool.md, AI 대화 기록은 exports/에서 확인하실 수 있습니다.

감사합니다.
{{APPLICANT_NAME}} 드림
```

이름은 계정 핸들에서 추정하지 않는다. 두 자리표시자는 실제 값으로 바꾼다. 메일은 최종 수신자·제목·본문 승인 후에만 발송한다. 도구가 없으면 완성된 본문을 전달하고 소유자가 발송하도록 안내한다. 실제 발송하지 않았으면 ‘초안 준비’로 보고한다.

최종 보고는 **배포 URL / 저장소·검증 commit / 실제 검사와 경고 / 미확인·한계 / 이메일 상태**만 정리한다. ‘모두 완벽’, ‘AI 티 제거 완료’, ‘무중단 보장’ 등의 검증하지 않은 평가는 쓰지 않는다.

## 8. README.md에 적용할 완성 문안

아래 블록을 README의 본문으로 사용한다. `{{DEPLOY_URL}}`을 실제 주소로 치환하고, 확인한 최종 코드와 다른 사실만 최소 수정한다. 링크 대상 `docs/verification.md#access-check-setup`과 `exports/README.md`는 §10·§11로 함께 완성한다.

````markdown
# 단풍

## 1. 프로젝트 개요 및 배포 URL

단풍은 짧은 글을 올리고, 댓글과 답글로 대화하며, 관심 있는 사람을 팔로우하는 SNS입니다. 글과 공개 프로필은 로그인하지 않아도 읽을 수 있습니다. 글을 남기거나 다른 사람을 팔로우하려면 가입과 프로필 설정이 필요합니다.

**[단풍 사용하기]({{DEPLOY_URL}})** · [GitHub 저장소](https://github.com/kimkangmin0914/8around-sns)

### 직접 사용해 보기

1. **가입하고 글 쓰기**에서 이메일과 비밀번호로 가입한 뒤 사용자명, 표시 이름, 소개를 입력합니다. 사용자명이 겹치면 계정을 다시 만들지 않고 이름만 바꿔 저장합니다.
2. **전체 글**에서 글을 게시합니다. 새로고침한 뒤에도 같은 글이 남아 있는지 확인할 수 있습니다.
3. 다른 브라우저나 시크릿 창에서 별도 계정으로 로그인합니다. 첫 계정의 글에 댓글을 남기고, 첫 계정으로 돌아와 **답글 쓰기**를 누르면 대화를 이어갈 수 있습니다.
4. **사람들**에서 프로필을 방문해 팔로우합니다. **내 프로필 → 팔로잉**과 상대 프로필의 **팔로워**에서 같은 관계를 확인하고 해제할 수 있습니다.

가입 후에는 자동으로 로그인됩니다. 별도로 로그인을 확인하려면 로그아웃한 뒤 같은 이메일과 비밀번호로 다시 접속하면 됩니다. 같은 브라우저의 일반 탭끼리는 로그인 정보가 공유될 수 있으므로 두 계정은 분리된 창 환경에서 확인해 주세요.

이 앱은 8around 과제용입니다. 현재 이메일 소유권 확인과 메일을 통한 비밀번호 복구를 제공하지 않습니다. 다른 서비스에서 사용하는 비밀번호를 재사용하지 말고, 글과 프로필에 민감한 개인정보를 입력하지 말아 주세요. `[점검]`으로 표시한 글과 댓글은 동작을 확인하며 남긴 자료입니다.

## 2. 로컬 실행 방법

로컬 실행은 **내 컴퓨터에서 웹 앱을 켜는 것**을 뜻합니다. 계정과 글은 온라인의 Supabase에 저장하므로 인터넷 연결이 필요합니다. Docker나 PostgreSQL을 컴퓨터에 따로 설치하지 않습니다.

### 준비할 것

[Node.js](https://nodejs.org/en/download)의 **24 LTS**와 [Git](https://git-scm.com/install/)을 설치합니다. Node.js에는 패키지 설치 명령인 npm이 함께 제공됩니다. 터미널에서 다음 명령으로 설치를 확인할 수 있습니다.

```bash
node --version
npm --version
git --version
```

Codespaces를 이미 사용한다면 그곳의 터미널에서 같은 절차를 진행할 수 있습니다. 로컬 PC가 앱 실행을 감당하기 어려울 때의 대안이며, 무료 사용량은 본인 계정에서 확인해야 합니다.

### 소스 받기

새 폴더에서 다음 명령을 한 줄씩 실행합니다. 이미 소스를 받았다면 다시 복제하지 않고 기존 폴더로 이동합니다.

```bash
git clone https://github.com/kimkangmin0914/8around-sns.git
cd 8around-sns
npm ci
```

`npm ci`는 `package-lock.json`에 기록된 버전으로 필요한 라이브러리를 설치합니다. 설치가 끝나기 전에는 다음 명령을 실행하지 않습니다.

### 계정과 글을 저장할 곳 준비하기

직접 재현할 때는 [Supabase](https://supabase.com/)에 본인의 과제용 프로젝트를 만듭니다. 다른 서비스가 사용 중인 DB를 재사용하지 않습니다.

**새 프로젝트에서만**, Supabase의 **SQL Editor → New query**에 [`202609170001_initial.sql`](supabase/migrations/202609170001_initial.sql) 전체를 넣고 한 번 실행합니다. 이 파일은 사용자 프로필, 글, 댓글, 팔로우를 저장할 표와 접근 규칙을 만듭니다. DB 구조를 파일로 남기는 방식을 *마이그레이션*이라고 합니다.

이미 이 앱에 연결해 사용 중인 프로젝트라면 초기 SQL을 다시 실행하지 않습니다. 같은 이름의 표가 있다는 오류가 나도 삭제하거나 초기화하지 말고, 적용 이력부터 확인합니다.

인증 설정에서는 **신규 가입 허용**과 **Email 제공자**를 켜고, **Confirm Email**과 **익명 로그인**은 끕니다. 현재 앱은 확인 메일을 기다리는 흐름을 제공하지 않기 때문입니다. 로그인 시도 등을 제한하는 기본 보호 설정은 그대로 둡니다. 메뉴 위치가 바뀌면 [Supabase 인증 설정 안내](https://supabase.com/docs/guides/auth/general-configuration)에서 같은 설정 이름을 찾을 수 있습니다.

### 앱에 연결 정보 넣기

프로젝트 폴더의 `.env.example`을 복사해 같은 위치에 `.env.local`을 만듭니다. 이미 파일이 있다면 덮어쓰지 않습니다. 아래 두 항목을 본인의 Supabase 프로젝트 값으로 바꿉니다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY
```

첫 항목은 **Data API의 프로젝트 URL**, 두 번째는 **API Keys의 publishable key**입니다. 키는 `sb_publishable_`로 시작해야 합니다. DB 비밀번호나 `secret`·`service_role` 키를 넣지 않습니다.

이 두 값은 앱이 연결할 곳을 알려 줍니다. 글을 쓸 수 있는지는 별도로 로그인 정보와 DB 규칙이 결정합니다. 실제 `.env.local`은 Git에서 제외되며, 다른 사람에게 파일째 보내지 않습니다.

### 실행하기

```bash
npm run dev
```

브라우저에서 `http://localhost:3000`을 엽니다. Codespaces에서는 **Ports의 3000번 전달 주소**를 사용합니다. 종료할 때는 터미널에서 `Ctrl+C`를 누릅니다. 앱을 종료해도 Supabase에 저장된 자료는 지워지지 않습니다.

`npm run dev`가 사용하는 `0.0.0.0` 설정은 Codespaces의 포트 전달에도 대응하기 위한 것입니다. 로컬 공유 네트워크에 개발 서버를 공개하지 않으려면 다음처럼 주소를 제한해 실행할 수 있습니다.

```bash
npx next dev --webpack --hostname 127.0.0.1
```

화면은 열리는데 글을 불러오지 못한다면 `.env.local`의 두 값, Supabase 프로젝트의 활성 상태, 초기 SQL 적용 여부를 차례로 확인합니다. 설정을 바꾼 뒤에는 앱을 다시 실행합니다. Windows PowerShell에서 `npm.ps1` 실행 제한이 나면 `npm.cmd ci`, `npm.cmd run dev`로 실행할 수 있습니다.

## 3. 기술 스택 선택 이유

**Next.js와 TypeScript**로 화면과 서버 처리를 한 프로젝트에 두었습니다. 글 작성에서 화면과 저장 처리를 함께 따라갈 수 있고, 수정할 저장소를 여러 개로 나누지 않아도 됩니다. TypeScript는 코드에서 잘못된 형태의 값을 사용하는 실수를 찾는 데 도움을 줍니다. 외부 입력과 로그인 확인은 서버에서 별도로 수행합니다.

**Supabase Auth와 PostgreSQL**을 사용해 로그인과 데이터 저장을 연결했습니다. 인증 기반을 직접 만드는 대신, 이 앱에 필요한 작성자 확인과 데이터 접근 규칙을 구현하는 데 집중했습니다. 내 컴퓨터가 꺼져도 저장된 글은 온라인 DB에 남습니다.

**Tailwind CSS, shadcn/ui의 Radix 기반 Button, Lucide, Pretendard**를 화면 구성에 사용했습니다. 필요한 부품만 가져오고 상단 탐색, 작성자와 본문의 정렬, 댓글 연결선은 앱에 맞게 구성했습니다. Pretendard는 앱이 직접 제공하므로 외부 글꼴 서버에 매번 요청하지 않습니다.

공개 앱은 **Netlify**에서 실행하도록 구성했습니다. 개발 중 사용하는 Codespaces 주소를 제출 주소로 쓰지 않습니다. 설치한 정확한 버전은 [`package.json`](package.json)과 [`package-lock.json`](package-lock.json), 출처와 라이선스는 [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md)에 있습니다.

## 4. 하네스 구성: 고친 뒤에도 다시 확인할 수 있도록

여기서 **하네스**는 코드를 바꿀 때 같은 규칙과 검사로 확인할 수 있게 준비한 작업 환경을 뜻합니다. 예를 들어 글의 길이 제한을 바꾸더라도, 공백 글이나 다른 사람 명의의 글까지 저장되도록 바뀌어서는 안 됩니다. 이런 실수를 변경 직후 찾는 데 목적을 두었습니다.

| 준비한 것                              | 사용하는 이유                                                                      |
| -------------------------------------- | ---------------------------------------------------------------------------------- |
| `AGENTS.md`와 `IMPLEMENTATION_PLAN.md` | 구현할 범위, 데이터 보호 규칙, 실제 검사 방법을 함께 읽도록 했습니다.              |
| `DESIGN.md`                            | 글자, 색, 간격과 화면 상태를 한곳에서 정해 화면마다 기준이 달라지지 않게 했습니다. |
| `npm run check`                        | 코드 형식, 의심스러운 코드, 타입과 작은 기능의 테스트를 순서대로 확인합니다.       |
| `npm run build`                        | 현재 소스가 배포 가능한 형태로 만들어지는지 확인합니다.                            |
| `npm run test:access`                  | 준비한 두 계정으로 실제 DB의 저장·조회·권한을 확인합니다.                          |
| GitHub Actions                         | `main`의 변경과 PR에서 설치·`check`·빌드를 같은 순서로 실행합니다.                 |
| `docs/verification.md`와 `exports/`    | 실제 확인 결과와 AI 작업 기록을 남겨 설명과 실행을 대조할 수 있게 했습니다.        |

일반적인 코드 수정 후에는 다음 두 명령을 실행합니다.

```bash
npm run check
npm run build
```

`check` 안의 일부 테스트는 서버나 DB의 응답을 대신 만들어 코드의 분기를 검사합니다. **이 테스트와 빌드가 통과했다고 실제 가입이나 DB 권한까지 확인된 것은 아닙니다.** 공개 사이트의 사용 흐름은 브라우저에서 따로 확인합니다.

`test:access`는 실제 글 2개와 댓글 2개를 추가하며, 두 점검 계정 사이에 팔로우가 없으면 관계 1개도 만듭니다. 기존 데이터를 지우거나 자동으로 청소하지 않습니다. 따라서 승인한 과제용 프로젝트와 두 계정을 준비한 뒤에만 실행합니다. 필요한 설정은 [실제 접근 검사 안내](docs/verification.md#access-check-setup)에 분리했습니다.

AI는 **OpenAI Codex**를 사용했습니다. 디자인 작업에는 Oh My Design의 세 스킬 지침을 읽고 적용했습니다. 전용 스킬 실행이나 별도 전문가 에이전트가 수행한 일처럼 표시하지 않았습니다. 사용 도구는 [`tool.md`](tool.md), 실제 대화 파일의 읽는 순서는 [`exports/README.md`](exports/README.md)에 정리합니다. 대화 요약을 원본 기록 대신 제출하지 않습니다.

## 5. 구현 중 내린 결정과 트레이드오프

### 가입 계정과 공개 프로필을 나눴습니다

가입은 됐지만 원하는 사용자명이 이미 사용 중일 수 있습니다. 이때 계정을 새로 만들게 하지 않고, 같은 계정에서 프로필 설정만 다시 시도하도록 했습니다. 이메일과 비밀번호는 다른 사람이 읽는 프로필에 포함하지 않습니다. 대신 현재 버전에서는 최초 설정 후 프로필을 수정할 수 없습니다.

### 버튼을 숨기는 데서 끝내지 않았습니다

글을 쓸 때 작성자는 화면이 보낸 이름이 아니라 서버가 확인한 로그인 사용자로 정합니다. DB에서도 사용자별 접근 규칙인 **RLS**와 입력 가능한 항목을 제한했습니다. 화면을 거치지 않는 요청에도 다른 사람 명의의 글을 저장하지 못하게 하기 위해서입니다.

팔로우는 A→B라는 관계를 한 번만 저장합니다. A의 팔로잉과 B의 팔로워는 그 같은 관계를 읽습니다. 별도 숫자를 따로 저장하지 않아 목록과 숫자를 맞춰 관리할 부담을 줄였습니다.

### 서버가 확인한 뒤 성공을 표시합니다

저장이 성공했다는 응답을 받기 전에는 작성 내용을 지우지 않습니다. 응답이 끊기면 저장 여부를 단정하지 않고, 초안을 남긴 채 목록을 다시 확인할 수 있게 했습니다. 화면에서 자동으로 글을 재전송하지는 않습니다. 다만 사용자가 다시 게시하면 같은 글이 중복될 가능성은 남아 있습니다.

### 대화와 목록의 범위를 제한했습니다

게시글과 댓글은 앞뒤 공백을 정리한 뒤 500자까지 받습니다. 내부에서는 Unicode 코드 포인트로 세므로, 일부 합성 이모지는 화면에 하나로 보여도 여러 자로 계산될 수 있습니다. 폼을 보낼 때 달라지는 줄바꿈은 같은 형태로 맞춰 화면과 서버의 계산 차이를 줄였습니다.

답글은 댓글에 한 단계까지 달 수 있고, 답글의 답글은 받지 않습니다. 서로 다른 게시물의 댓글이 부모로 연결되는 것도 막습니다. 목록은 20개씩 나누어 읽습니다. 그 사이에 새 글이 추가되면 페이지 경계에서 같은 글을 다시 볼 수 있으며, 실시간 갱신이나 읽던 목록의 완전한 고정은 제공하지 않습니다.

### 실행 구성을 작게 유지했습니다

개발 앱과 공개 앱은 같은 과제용 Supabase 프로젝트에 연결합니다. 설정을 줄인 대신 개발과 공개 데이터가 분리되지는 않습니다. 그래서 테스트에서 DB 초기화나 일괄 삭제를 하지 않습니다. 무료 서비스의 한도나 프로젝트 일시정지가 접속에 영향을 줄 수 있다는 점도 남아 있습니다.

이메일 확인과 메일 기반 계정 복구, 글·댓글의 수정·삭제, 이미지 업로드, 좋아요, 알림은 구현하지 않았습니다. 없는 기능을 버튼만 만들어 표시하지 않습니다.

## 6. 시간이 더 있었다면 개선했을 부분

**디자인을 더 다듬고 싶습니다.** 긴 글과 여러 답글을 휴대폰에서 읽을 때 간격이 답답하지 않은지, 작성과 취소 버튼을 찾기 쉬운지 실제 사용자와 확인하고 조정하겠습니다. 화면마다 별도의 장식을 더하기보다는 읽고 대화하는 과정이 편해지는 쪽을 우선하겠습니다.

**계정을 다시 찾고 정보를 고치는 기능을 보완하고 싶습니다.** 이메일 확인과 비밀번호 복구, 프로필 수정이 있으면 오기입이나 비밀번호 분실 뒤에도 서비스를 계속 사용할 수 있습니다. 실제 사용자 자료를 다루기 시작한다면 탈퇴와 데이터 삭제도 함께 설계해야 합니다.

**통신이 끊겨도 안심하고 게시할 수 있게 만들고 싶습니다.** 같은 작성 요청을 다시 보내도 글이 중복되지 않도록 처리하고, 새 글이 올라오는 중에도 읽던 목록을 더 안정적으로 이어갈 수 있게 개선하겠습니다.

실행별 결과와 아직 확인하지 못한 항목은 [검증 기록](docs/verification.md)에 남깁니다. 현재 CI 결과는 [GitHub Actions](https://github.com/kimkangmin0914/8around-sns/actions/workflows/ci.yml)에서 확인할 수 있습니다.
````

## 9. tool.md에 적용할 완성 문안

아래 문안을 사용한다. 기록된 버전과 실제 P4 사용 버전이 다르면 둘을 구분해 적고, 사용하지 않은 도구는 추가하지 않는다. ChatGPT는 설계·검토 역할이며 앱을 직접 구현·배포한 것으로 적지 않는다. 내보내기 방식의 수용 여부는 확인한 경우에만 그 결과로 교체한다.

```markdown
# 사용한 도구

## AI 작업과 디자인

| 도구                                 | 실제 사용한 일                                                                                                                                      |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| ChatGPT                              | 과제 해석, 구현·교정 작업서와 README 초안 작성, 공개 저장소·CI 대조 검토에 사용했습니다. 앱을 직접 수정하거나 공개 배포한 것으로 기록하지 않습니다. |
| OpenAI Codex CLI 0.154.0             | 요구사항과 코드를 읽고, 기능 구현·오류 수정·검사 실행을 진행했습니다.                                                                               |
| Codex의 셸·파일 수정·웹 조회 도구    | 파일 변경, 패키지 설치, 빌드와 검사를 실행하고 관련 공식 문서를 확인했습니다.                                                                       |
| OpenAI Docs 스킬                     | Codex 실행 기록과 내보내기 방법을 확인하는 데 사용했습니다.                                                                                         |
| Oh My Design CLI 2.0.0의 `omd:apply` | `DESIGN.md`를 기준으로 기존 화면을 수정했습니다.                                                                                                    |
| 같은 배포물의 `omd:feel`             | 입력 경계, 색 대비, 조작 영역, 포커스와 움직임 감소를 점검했습니다.                                                                                 |
| 같은 배포물의 `omd:slop-audit`       | 반복된 홍보 문구와 장식을 줄이고, 같은 화면의 수정 전후를 비교했습니다.                                                                             |
| Playwright 1.63.0와 Chromium         | 개발용 검사 환경에서 로그인, 게시, 답글, 팔로우, 모바일 화면과 실패 상황을 확인했습니다. 앱 실행 의존성에는 넣지 않았습니다.                        |

Oh My Design의 세 `SKILL.md`는 Codex에서 읽을 수 있는 위치에 등록되어 있습니다. 별도의 스킬 호출 API가 없어 문서의 지침을 직접 적용했습니다. 하위 전문가 에이전트나 전체 자동화 파이프라인을 실행한 것으로 기록하지 않습니다. 실제 사용한 CLI는 2.0.0이며, 계획에 한때 적혔던 2.0.1로 바꾸어 쓰지 않습니다.

디자인 참고는 OMD의 Karrot 항목과 SEED 제품 지침입니다. 작성자와 콘텐츠의 읽는 순서, 행동에 대한 피드백을 참고했습니다. 당근의 브랜드 자산이나 거래 화면을 복제하지 않았고, SEED 패키지를 앱에 설치하지 않았습니다. 채택한 색·간격·화면 규칙은 [DESIGN.md](DESIGN.md)에 있습니다.

앱 구현에 추가 MCP, 병렬 에이전트, 이미지 생성 도구, 유료 디자인 API는 사용하지 않았습니다. 완성된 SNS가 글을 처리할 때 AI 모델을 호출하지도 않습니다.

## 앱과 검사 라이브러리

화면과 서버에는 Next.js·React·TypeScript, 인증과 저장에는 Supabase Auth·PostgreSQL을 사용했습니다. UI에는 Tailwind CSS, shadcn/ui에서 가져온 Radix 기반 Button, Lucide, Pretendard를 사용했습니다. 형식과 코드 검사는 Prettier·ESLint·TypeScript·Vitest로 구성했습니다.

정확한 의존성 버전은 [package.json](package.json)과 [package-lock.json](package-lock.json), 출처와 라이선스는 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)에 보존합니다. `AGENTS.md`는 작업 규칙, `IMPLEMENTATION_PLAN.md`는 기능 계약, `DESIGN.md`는 화면의 기준으로 사용했습니다.

## 실제 대화 기록

[exports/README.md](exports/README.md)에 세션별 파일과 범위를 정리합니다. 공개 파일은 실제 Codex 세션에 남은 입력·응답·도구 결과에서 만들었으며, 나중에 작성한 요약으로 대화를 대신하지 않았습니다.

`scripts/export-session.mjs`는 세션 식별자와 원본 해시로 파일명을 만들어 서로 다른 세션과 스냅샷을 보존합니다. 비공개 원본은 Git에서 제외한 `.ai-raw/`에 남깁니다. 공개 전에는 설정값과 토큰, 개인정보를 점검하고, 검사할 수 없는 이미지 첨부는 제외 표시로 바꿉니다. 임의의 모든 인코딩에서 민감한 값을 자동 검출한다고 보장하지는 않습니다.

현재 기록 형식은 `filtered-codex-rollout`입니다. 확인한 CLI에는 전용 transcript export 명령이 없어 기존 실제 세션을 변환했습니다. 공식 내보내기와 같은 형식이라고 주장하지 않으며, 과제의 export 방식으로 수용되는지는 별도로 확인해야 합니다. 형식 확인 결과와 실제 검사 범위는 [검증 기록](docs/verification.md)에 남깁니다.
```

## 10. docs/verification.md 정리

기존 실행 기록은 삭제하거나 사후에 성공 기록으로 고치지 않는다. 상단에는 아래 요약을 둔다. 아래 문안은 확인된 P3.5 상태로 작성되어 있다. P4에서 실제 확인한 결과만 해당 셀과 문장에 갱신하고, 과거 상세 기록은 `<details>` 아래로 묶는다. 기존 내부 링크는 유지한다.

````markdown
# 검증 기록

## 현재 확인한 상태

공개 배포 전 기준 코드는 `2539ed35691885ca17f46c7fc17296bb6ff8ceb5`입니다. 이 커밋에는 P3.5 수정, 서비스명 ‘단풍’, 화면 문구 정제가 반영되어 있습니다.

| 확인 대상                           | 결과와 근거                                                                                                                                                                              |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 코드 설치·검사·빌드                 | [GitHub CI 35228741200](https://github.com/kimkangmin0914/8around-sns/actions/runs/35228741200)에서 `npm ci`, `npm run check`, `npm run build` 성공. 11개 테스트 파일의 120개 검사 통과. |
| 기존 실제 앱 검사                   | 개발 환경에서 두 계정의 로그인·작성·댓글·팔로우와 실패 처리를 확인했습니다. 아래 상세 기록과 구분해 읽어 주세요.                                                                         |
| 공개 배포 URL·배포 ID               | 아직 확인하지 않았습니다.                                                                                                                                                                |
| 새 외부 사용자의 가입부터 전체 체험 | 아직 확인하지 않았습니다. 기존 HTTP 가입과 개발 브라우저 검사를 외부 체험으로 대신하지 않습니다.                                                                                         |
| 대화 내보내기 방식                  | 실제 Codex 세션을 변환한 `filtered-codex-rollout`입니다. 과제에서 요구한 export 방식으로 수용되는지는 확인이 필요합니다.                                                                 |

CI에는 실제 DB와 점검 계정이 들어가지 않습니다. 따라서 CI 통과, 실제 DB 권한 검사, 외부 브라우저 체험은 서로 다른 결과입니다.

기준 CI에는 OMD 원본 스크립트의 미사용 함수 경고 1개와 ESLint 지원 종료·선택 설치 스크립트 관련 안내가 남아 있습니다. 오류와 경고를 구분해 기록했으며, 이를 없애기 위해 앱 검사를 끄지는 않았습니다.

## 공개 주소에서 확인할 흐름

| 행동                                      | 공개 배포 확인 상태 |
| ----------------------------------------- | ------------------- |
| 새로운 계정으로 가입하고 프로필 설정      | 미검증              |
| 로그아웃·재로그인·새로고침 후 로그인 유지 | 미검증              |
| 게시글 작성, 별도 계정으로 같은 글 읽기   | 미검증              |
| 댓글과 그 댓글의 답글 저장·조회           | 미검증              |
| 팔로우·해제 후 두 목록과 숫자 확인        | 미검증              |
| 모바일 배치, 키보드, 답글 취소 후 포커스  | 미검증              |
| 개발 환경을 중지한 뒤에도 정식 주소 접속  | 미검증              |

실제로 수행한 뒤에는 확인한 코드·배포 ID, 시각과 시간대, 사용한 계정 역할, 결과를 적습니다. 실제 사용자명이나 자격 증명은 적지 않습니다. 실행하지 못한 항목은 미검증으로 남깁니다.

<a id="access-check-setup"></a>

## 실제 DB 접근 검사 준비

`npm run test:access`는 화면을 조작하는 대신 실제 Supabase에 요청을 보내 규칙을 확인하는 명령입니다. 본인의 과제 프로젝트와 프로필 설정까지 완료한 서로 다른 점검 계정 A·B를 준비합니다. 앱 실행은 필요하지 않습니다.

프로젝트 루트의 Git 제외 파일 `.env.test.local`에 다음 항목을 넣습니다. 이미 파일이 있다면 덮어쓰지 않습니다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=https://CONFIRMED_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY
ACCESS_CHECK_PROJECT_REF=CONFIRMED_PROJECT_REF
ACCESS_CHECK_EMAIL_A=CHECK_ACCOUNT_A_EMAIL
ACCESS_CHECK_PASSWORD_A=CHECK_ACCOUNT_A_PASSWORD
ACCESS_CHECK_EMAIL_B=CHECK_ACCOUNT_B_EMAIL
ACCESS_CHECK_PASSWORD_B=CHECK_ACCOUNT_B_PASSWORD
```

`ACCESS_CHECK_PROJECT_REF`는 URL에서 `.supabase.co` 앞에 붙는 프로젝트 식별자입니다. 주소와 별도 확인값이 같은지 검사해 다른 DB를 잘못 점검하는 일을 줄입니다. publishable key만 사용하며 관리자 키는 허용하지 않습니다.

```bash
npm run test:access
```

매 실행은 표식이 있는 게시글 2개·댓글 2개를 추가합니다. A→B가 없다면 관계 1개도 만듭니다. 기존 자료를 초기화하거나 삭제하지 않으며, 새 계정을 대신 만들어 주지도 않습니다. 실행 전에 대상과 추가량을 확인해 주세요.

이 결과만으로 공개 사이트의 가입 화면, 쿠키, 버튼과 포커스까지 확인되는 것은 아닙니다. 브라우저의 전체 흐름은 별도로 점검합니다.
````

추가 실제 기록의 최소 항목: 확인 시각·시간대, 앱 commit, 배포 ID·URL, 실행 명령/행동, 결과, 남은 경고, 추가 데이터 수량·잔존 여부. 실제 OS IME와 네이티브 확대를 수행하지 않았다면 과거와 마찬가지로 미검증을 유지한다. 최종 파일에 불필요한 기록 양식 안내를 남기지 않으며 기존 상세 기록에 사실을 덧붙인다.

## 11. exports/README.md 완성

다음 본문을 사용하고 실제 파일 목록에서 읽을 순서 표만 작성한다. 첫·마지막 이벤트 시각, 세션·원본 hash, 포함 범위는 실제 메타데이터와 기록을 읽어 확인한다. 파일명 hash의 사전순을 시간순으로 오해하지 않는다.

```markdown
# AI 작업 기록

이 폴더에는 과제 진행에 사용한 실제 대화 기록을 보관합니다. 이 안내는 파일을 찾기 위한 목차이며 원본 대화를 대신하지 않습니다.

한 번 이어서 작업한 대화가 같은 세션입니다. 같은 세션을 여러 번 내보내면 앞선 대화가 다음 파일에도 포함될 수 있습니다. 최신 스냅샷과 앞선 파일을 구분해 표에 표시합니다. 서로 다른 세션은 따로 남깁니다.

<!-- ACTUAL_EXPORT_INDEX -->

Codex의 `filtered-codex-rollout` 파일에는 실제 사용자 입력, 응답, 도구 호출과 결과가 담겨 있습니다. 시스템 지침·내부 추론·중복 이벤트는 제외하며, 비공개 원본은 별도로 보관합니다. 개인정보·설정값·토큰을 정리한 자리와 제외한 이미지 첨부는 표시를 남깁니다. 자동 처리가 모든 인코딩의 비밀을 찾아낸다고 보장하지는 않습니다.

현재 형식은 Codex의 공식 transcript export가 아니라 기존 세션 기록을 변환한 것입니다. 과제의 export 요건 수용 여부는 [검증 기록](../docs/verification.md)에 구분합니다. 다른 AI 도구의 내보내기 파일은 해당 도구와 생성 방식을 별도로 표시합니다.

구현 결과를 먼저 보려면 [README](../README.md)를, 검사 결과를 보려면 [검증 기록](../docs/verification.md)을 읽어 주세요.
```

`ACTUAL_EXPORT_INDEX`를 실제 Markdown 표로 바꾼다. 열은 ‘읽을 순서 / 파일 링크 / 실제 대화 범위 / 앞선 파일과의 관계’로 제한한다. 큰 파일은 기록을 읽는 별도 스크립트로 처리하되 비밀을 출력하지 않는다. 새 P4 스냅샷과 확보한 관련 ChatGPT 기록을 포함하고, 미확보·미확인 범위를 숨기지 않는다.

## 12. 최종 문서 검사

- 루트 README의 여섯 필수 제목과 배포 URL이 실제로 있는지 검사한다. `read.md`라는 별도 대체 파일은 만들지 않는다.
- 서비스 소개, 로그인 정책, 수정·삭제 제한, 코드 포인트 한도, 클라우드 DB 연결이 실제 코드와 일치하는지 확인한다.
- README의 개별 명령이 `package.json` 또는 설치한 공식 도구에 존재하는지 확인한다. 없는 `doctor`, `verify:ci`, `db:start` 등을 옛 계획에서 복사하지 않는다.
- Markdown 링크·코드 블록·표·앵커를 확인하고, 없는 이미지·파일 링크는 제거하거나 실제 파일로 연결한다.
- 실제 값이 필요한 `{{DEPLOY_URL}}`, `{{APPLICANT_NAME}}`, `ACTUAL_EXPORT_INDEX`를 작업 대상 문서와 이메일에 남기지 않는다. 예시 환경변수 값은 예시로 유지한다. 사용자 입력이 필요한 실제 이름과 주소는 추정하지 않는다.
- 과거 검사 수·commit 대기 문장을 현재 사실로 표시하지 않는다. 상세 원시 대화·과거 실행 기록은 문체 정제를 이유로 재작성하지 않는다.
- 사람이 읽는 부분을 한 번 연속해서 읽는다. 전문용어를 제거해 의미가 바뀐 문장, 같은 결론의 반복, 실제 행동 없는 홍보 문구를 고친다. 미래 개선점을 이미 구현한 기능처럼 쓰지 않는다.
- 최종 결과는 §7의 종료 기준으로 판정한다. 공개 URL·신규 외부 가입·필수 흐름·내보내기 형식·메일 발송 중 미확인이 있으면 해당 상태를 구분해 보고한다.
