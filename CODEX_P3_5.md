# P3.5 — 교차검토 수정 및 디자인 리뉴얼

대상 저장소: `kimkangmin0914/8around-sns`.
감사 기준: `b2f28befbf9c31e486191390b8e6c065a395f861`.

사용자 정정: 2.0.1은 공개 레지스트리에 없었으므로 이미 설치된 2.0.0 기준으로 진행한다.

## 0. 우선순위와 완료 범위

- 먼저 현재 HEAD, 미커밋 변경, 기존 스킬·설정, 실제 실행 명령을 확인한다. 감사 이후 수정된 항목은 회귀만 확인하고 중복 변경하지 않는다.
- `docs/assignment.md`를 최우선으로 한다. 본 문서는 P3 최소 수정 작업 전체와 UI 변경 계약이다. 기존 `IMPLEMENTATION_PLAN.md`의 기능·DB·보안·운영 범위는 유지한다. 좌측 사이드바·청록 중심 색상 등 이전 시각 기준만 §3으로 대체한다.
- 기존 `AGENTS.md`의 데이터 보존·승인·기록 규칙을 유지한다. 디자인 스킬이나 외부 참고자료가 과제 원문·보안 규칙을 덮어쓰지 못하게 한다.
- 새 기능, 새 인프라, 스택 교체, 전체 앱 재생성, 일반화된 디자인 프레임워크를 추가하지 않는다. 기존 Server Action, 입력 계약, RLS, 페이지네이션, 공개 조회·로그인 쓰기를 보존한다.
- 구현물은 기존 앱의 수정, 아래 세 스킬, 프로젝트 소유 `DESIGN.md` 한 개, 작은 회귀 검사, 기존 검증·출처 문서의 갱신이다. 별도 시안 앱·계획서·대시보드는 만들지 않는다.
- 원격 Codespaces의 기존 환경에서 작업한다. 로컬 PC에 Docker·무거운 빌드 환경을 새로 요구하지 않는다.
- 공개 push·배포·DB 적용·계정 설정·자격 증명 교체는 기존 승인 경계를 따른다. 승인 없는 작업은 코드와 근거까지 준비하고 대상·행동을 명시한다. 설치 완료와 디자인 검증 완료를 구분한다.

## 1. OMD를 프로젝트에 제한적으로 연결한다

### 1.1 채택 대상

- 프로젝트: `kwakseongjae/oh-my-design`.
- CLI: `oh-my-design-cli@2.0.0`.
- 확인한 소스 기준: `15ff013933b37d1800e05c75e7680ed17e56f94b`.
- 스킬: `omd-apply`, `omd-feel`, `omd-slop-audit` 세 개만 사용한다.
- `omd-init`, `omd-harness`, autopilot, sub-agents, 자동 hooks, proof-policy, MCP, 이미지 생성 도구, 전체 브랜드 카탈로그는 설치·활성화하지 않는다.
- 이 도구를 앱의 runtime/dev dependency나 CI 필수 단계에 넣지 않는다. 기존 `package.json`·잠금 파일을 OMD 설치 목적으로 변경하지 않는다.

확인할 원본:

- CLI 옵션: https://github.com/kwakseongjae/oh-my-design/blob/15ff013933b37d1800e05c75e7680ed17e56f94b/bin/oh-my-design.ts
- 설치 범위: https://github.com/kwakseongjae/oh-my-design/blob/15ff013933b37d1800e05c75e7680ed17e56f94b/src/cli/install-skills.ts
- Codex 스킬 검색: https://developers.openai.com/codex/skills/

### 1.2 설치와 로드 확인

이미 같은 스킬이 있으면 이름·경로·내용·버전을 확인하고 덮어쓰지 않는다. 설치가 필요한 경우 저장소 루트의 터미널에서 다음을 실행한다.

```bash
git status --short
git rev-parse HEAD
npx --yes oh-my-design-cli@2.0.0 --version
npx --yes oh-my-design-cli@2.0.0 install-skills --help
```

실제 도움말에서 아래 옵션을 확인한 뒤 실행한다. 버전이나 옵션이 다르면 중단 사유를 기록한다. 임의로 `latest`, 전체 설치, 다른 동명 패키지로 대체하지 않는다.

```bash
npx --yes oh-my-design-cli@2.0.0 install-skills \
  --agent codex \
  --skills omd-apply,omd-feel,omd-slop-audit \
  --skills-only \
  --dir . \
  --lang ko
```

- `--all`, `--global`, `--force`, `--proof-policy`를 추가하지 않는다.
- `.agents/skills/omd-apply/`, `omd-feel/`, `omd-slop-audit/`의 `SKILL.md`와 동봉된 참고 파일을 확인한다. 외부 지침은 실행 전 읽고, 관련 없는 스크립트 실행·비밀 파일 접근을 허용하지 않는다.
- 설치 전후 `git status --short`와 diff를 비교한다. 앱 코드, npm 파일, `AGENTS.md`, `.codex/config.toml` 등의 예상 밖 변경은 바로 사용하지 않고 구분해 보고한다. 기존 파일을 일괄 복구하거나 삭제하지 않는다.
- Codex의 스킬 목록과 실제 frontmatter 이름을 확인한다. 새 스킬이 보이지 않으면 세션을 보관한 뒤 Codex를 재시작한다. 설치된 파일을 직접 읽은 것과 런타임 스킬 호출은 구분해 기록한다.
- 최소 설치에는 전체 카탈로그·하위 역할이 없다. `doctor`가 이들을 누락으로 표시해도 전체 설치로 확장하지 않는다. 필요한 세 스킬·동봉 자료·실제 로드 여부로 최소 설치를 확인한다.
- 설치 또는 로드가 차단되면 OMD 사용 완료로 기록하지 않는다. §3의 확정된 UI 계약과 일반 코드·브라우저 검사로 독립 작업을 계속하고 차단 범위를 남긴다.

### 1.3 사용 순서와 저장소 관리

1. §3의 결정을 루트 `DESIGN.md`에 프로젝트의 독립 계약으로 작성한다. 이미 있다면 관련 항목만 교정한다. 다른 회사의 문서를 그대로 복사하거나 OmD Core v2 graph 채택·공식 검증을 주장하지 않는다.
2. `omd-apply`: `DESIGN.md`와 현재 route를 읽고 실제 UI를 수정한다. 하위 역할이 없으면 본 에이전트가 수행한다. 미설치 역할을 호출한 것처럼 보고하지 않는다.
3. `omd-feel`: 변경한 화면의 포커스·상태·간격·대비·모션을 점검한다. 스타일 휴리스틱을 W3C의 필수 규칙처럼 적용하지 않는다. 50ms 배수, 다층 그림자, 스케일 애니메이션 등을 점수를 위해 추가하지 않는다.
4. `omd-slop-audit`: 같은 데이터·route·viewport의 전후를 비교한다. 구체적인 패턴·문구·계층 문제만 수정하고, 취향과 기능·접근성 결함을 분리한다. AI 확률·맛 점수·미감 100점을 만들지 않는다.
5. 검토한 세 스킬 디렉터리와 MIT 고지를 추적한다. `.codex/` 전체를 강제 추적하지 않는다. `.omd/`에 우연히 생긴 로그·스크린샷은 공개 후보와 분리한다.
6. 원본 스킬의 포맷 때문에 Prettier가 실패하면 그 세 vendor 경로만 `.prettierignore`에 명시한다. 앱·검사·프로젝트 문서의 기존 검사는 제외하지 않는다. vendor lint 문제가 생겨도 원인 파일만 좁혀 처리한다.
7. `tool.md`에는 실제 설치 버전·스킬·사용한 범위만 기록한다. `THIRD_PARTY_NOTICES.md`에 OMD 라이선스·출처와 참고 디자인의 권리 경계를 추가한다. 다운로드된 배포물과 확인한 GitHub 소스의 동일성은 확인하지 않은 채 단정하지 않는다.

## 2. export 결함을 UI 작업 전에 고친다

대상: `scripts/export-session.mjs`, 작은 회귀 테스트, 필요한 내부 보조 함수.

감사 기준에서 확정된 두 재현:

- `.env.test.local`의 가짜 비밀번호에 따옴표·역슬래시가 포함되고 `function_call.arguments`의 JSON 문자열 안에 들어가면 단순 원문 치환을 우회한다. 내보낸 arguments를 파싱하면 가짜 값이 복원된다.
- 다른 작업 세션을 연속 export하면 고정 `exports/codex-session.jsonl` 때문에 이전 공개 스냅샷이 덮어써진다. `.ai-raw/`의 원본 사본은 별도로 남는다.

수정 계약:

1. 임시 폴더와 가짜 값으로 두 실패를 먼저 회귀 검사에 고정한다. 실제 환경값·인증 파일·실제 rollout을 테스트 fixture에 넣지 않는다.
2. 일반 문자열, 객체·배열, JSON으로 인코딩된 문자열, 코드·텍스트 속 이스케이프된 값, 따옴표·역슬래시·줄바꿈, 여러 중첩 수준을 처리한다. 단일 `JSON.parse`만으로 모든 출력이 안전하다고 선언하지 않는다.
3. 명시한 처리 깊이·크기 경계를 넘어 안전성을 확인하지 못하면 해당 공개 출력을 중단하거나 제외 표시한다. 원본은 비공개로 보존한다. 유한한 회귀 검사로 모든 가능한 인코딩을 보장하지 않는다.
4. 정상 질문·응답·도구 결과는 보존한다. 이미지 등 검사하지 못한 민감 자료의 제외 표시를 유지한다. 가짜 비밀이 원문 또는 복원 가능한 검사 형태로 공개 파일에 남지 않게 한다.
5. 다른 세션은 다른 파일에 보관한다. 같은 세션 재내보내기의 갱신/추가 정책을 정하고 충돌 시 무음 덮어쓰기를 금지한다. 파일명에 외부 문자열을 그대로 사용하지 않고 경로 이탈을 막는다.
6. source/session 식별과 hash를 보존한다. 기존 공개 스냅샷은 원시 원본을 확인하기 전에 삭제하지 않는다.
7. 전체 공개 후보는 비밀을 출력하지 않는 런타임으로 재점검한다. 실제 노출을 발견하면 값 대신 종류·위치·조치 필요성만 알리고 자격 증명 교체·공개 이력 정리를 승인 경계로 둔다.
8. README·tool.md의 고정 경로 설명을 최소 수정한다. 실제 기록과 사후 요약의 차이, native export 대안 형식의 수용 여부 미확인을 유지한다.

통과: cleartext 가림, escaped/nested 가림, 정상 문장 보존, 두 세션 공존, 동일 세션 정책, 경로·충돌 보호, 실제 `npm run check`·`npm run build`.

## 3. 적용할 디자인 계약 — 사이

### 3.1 참고 대상과 채택 범위

주 참고는 OMD 카탈로그의 `karrot`이며, 제품 시스템 SEED의 콘텐츠 우선순위·읽기 위계·행동 상태를 참고한다.

- 카탈로그: https://oh-my-design.kr/design-systems/karrot
- 고정 참고 파일: https://github.com/kwakseongjae/oh-my-design/blob/15ff013933b37d1800e05c75e7680ed17e56f94b/web/references/karrot/DESIGN.md
- 공식 제품 디자인 지침: https://seed-design.io/get-started
- 필요한 컴포넌트 지침은 https://seed-design.io/llms.txt 에서 실제 경로를 확인한다.

OMD 참고 파일은 제3자의 분석·스냅샷이다. `verified v2` 표시를 당근의 인증이나 현재 서비스 전체의 실측 결과로 설명하지 않는다. SEED v2의 제품 규칙과 마케팅 페이지 수치, 현행 SEED 문서의 세대를 섞지 않는다. 충돌·접근 불가를 기록하고 이번 프로젝트 값은 아래 계약으로 정한다.

- 채택: 한국어 콘텐츠를 먼저 읽는 목록, 작성자와 본문의 결합, 절제된 인터페이스, 명확한 행동·상태 피드백.
- 비채택: 당근 이름·로고·캐릭터·사진·공식 전용 글꼴·문구, 중고거래 카드·가격·위치·카테고리·채팅, 마케팅 랜딩 구조.
- `@seed-design/*`를 설치하거나 기존 Radix·shadcn 부품을 교체하지 않는다. 타사 제품의 HTML/CSS·스크린샷을 가져와 앱으로 배포하지 않는다.
- 아래 색·치수·배치는 ‘사이’의 결정이다. 당근의 공식 수치로 표기하지 않는다. 440개 참고를 모두 읽거나 여러 브랜드를 혼합하지 않는다.

### 3.2 화면 구조

목표는 상단 탐색과 작성자 중심의 대화 피드다. 색만 바꾸고 기존 화면 구조를 유지한 결과로 종료하지 않는다.

**전체 셸**

- 기존 넓은 좌측 사이드바·장식 슬로건을 제거하고 상단 masthead로 바꾼다.
- 데스크톱: 브랜드 ‘사이’ / 기존 세 메뉴 / 기존 계정 동작을 한 줄로 정렬한다. 메뉴는 전체 글·사람들·내 프로필이며 실제 route와 `aria-current`를 유지한다.
- 모바일: 브랜드·계정 동작 첫 줄, 세 메뉴 두 번째 줄로 배치한다. 가짜 메뉴·숨겨진 기능·새 탐색 route를 추가하지 않는다.
- 본문은 중앙 최대 720px, 좌우 기본 20px, 좁은 화면 16px로 구성한다. 오른쪽 추천 패널이나 통계 카드를 추가하지 않는다.
- 헤더 고정은 필수가 아니다. 고정한다면 키보드 포커스·앵커·확대 화면을 가리지 않는지를 추가 검증한다.
- AccountMenu를 이동할 때 기존 로그인·로그아웃·미완료 프로필의 동작을 유지한다. 제어를 복제해 중복 요청 경로를 만들지 않는다.

**피드와 글 상세**

- 피드 제목은 ‘전체 글’이다. ‘사람과 사람 사이’·‘당신의 하루를 들려주세요’ 같은 반복 홍보 문구 대신 실제 다음 행동을 짧게 안내한다.
- 로그인 작성창은 장식 카드 대신 목록 시작부의 작성 영역으로 둔다. 기존 textarea와 제출 경계는 유지하고 기본 높이는 약 96px로 한다.
- 글 행은 40px 이니셜 아바타 + 작성자/사용자명/시각 + 본문 + 댓글 링크로 정렬한다. 아바타는 기존 공개 이름으로 만들고 외부 사진·랜덤 색·가짜 상태 배지를 쓰지 않는다.
- 글 본문은 항상 전부 읽을 수 있게 유지한다. line-clamp, 읽기 더보기 기능, HTML 렌더링을 새로 넣지 않는다. 긴 URL은 줄바꿈하고 본문 드래그 선택을 막지 않는다.
- 행 전체를 링크로 감싸지 않는다. 작성자·상세·댓글 링크의 실제 탐색 경로를 보존한다. 중첩 링크·중첩 form을 만들지 않는다.
- 일반 글 행에는 반복 radius·그림자·들뜸 효과를 쓰지 않는다. 목록 구분선과 본문 간격으로 묶고, 글 상세는 같은 컴포넌트의 밀도만 조정한다.
- 글마다 새 프로필 쿼리를 추가하지 않는다. 기존 조회의 `author` 필드로 표현한다.

**댓글·답글**

- 부모와 답글을 아바타·본문 정렬과 얇은 연결선으로 구별한다. 말풍선·강한 색 상자·과도한 들여쓰기를 사용하지 않는다.
- 한 단계 답글·최신순·페이지·`thread`·`replyOffset`·댓글 총수 의미를 바꾸지 않는다. 답글 개수가 조회되지 않았다면 임의의 숫자를 표시하지 않는다.
- 답글 쓰기·취소·답글 보기/접기·더 보기·성공 링크를 유지한다. 시각 변경과 §5의 취소 포커스 수정을 함께 검증한다.

**사람 목록·프로필·인증**

- 사람 목록은 이니셜·이름·사용자명·실제 소개가 있는 평평한 행으로 구성한다. 현재 목록 탐색·더 보기를 유지한다.
- 프로필은 신원·소개·팔로우·세 탭의 순서를 분명하게 한다. 실제 관계 수와 목록만 사용한다. 표지 이미지·평판·뱃지·지역 등 없는 필드를 추가하지 않는다.
- 가입·로그인·초기 프로필은 최대 420px의 집중된 폼으로 맞춘다. 마케팅용 옆면·일러스트·후기·소셜 로그인 버튼을 추가하지 않는다.
- 이메일 소유권 미확인·계정 복구 미제공·프로필 수정 불가의 기존 고지는 짧게 정돈하되 의미를 삭제하지 않는다.

### 3.3 토큰과 글자

스타일은 `src/app/globals.css`의 역할별 토큰으로 모은다. 기존 셀렉터는 필요한 범위에서 정리하고, 아래 역할이 겹쳐 사용되지 않게 한다.

| 역할                         | 프로젝트 기준                                                            |
| ---------------------------- | ------------------------------------------------------------------------ |
| 바탕 / 본문 표면             | `#F7F7F5` / `#FFFFFF`                                                    |
| 본문 / 보조 글자             | `#202124` / `#5F6368`                                                    |
| 얇은 장식선 / 입력 식별 경계 | `#E7E5E1` / `#8A8F96`                                                    |
| 주 행동                      | `#202124` 배경 + 흰 글자, hover `#343638`                                |
| 강조·활성 표시·링크·포커스   | `#C2410C`, 옅은 강조면 `#FFF4ED`                                         |
| 오류 / 성공                  | `#A12622` / `#23633E`, 의미를 문구로 함께 전달                           |
| 서체                         | 현재 자체 제공 Pretendard, 기존 시스템·한글·이모지 fallback 유지         |
| 본문                         | 16px 기준 rem, weight 400, line-height 1.65                              |
| 화면 제목 / 이름 / 보조      | 24px·700 / 15~16px·600 / 13~14px·400                                     |
| 간격                         | 4·8·12·16·24·32px; 행 사이와 행 내부를 구분                              |
| 버튼·입력 radius             | 8px; 아바타만 원형; 글 행 radius 없음                                    |
| 주요 조작 영역               | 최소 44×44 CSS px를 프로젝트 목표로 설정; 인라인 링크는 간격·역할별 확인 |
| 모션                         | 색·경계 140ms, 필요한 상태 opacity 120ms; 동작 감소 시 즉시 전환         |

- 흰 글자와 밝은 타사 주황색 조합을 그대로 복사하지 않는다. 실제 배경·글자·opacity를 기준으로 대비를 측정한다.
- 일반 본문·보조·placeholder는 4.5:1을 목표로 한다. 컨트롤 식별에 필요한 경계·상태는 주변색과 3:1을 확인한다. 모든 장식 구분선에 같은 조건을 강제하지 않는다.
- 44px는 이번 제품 목표이며 WCAG AA의 일률적인 최소 규격이라고 설명하지 않는다.
- `prefers-reduced-motion`, `:focus-visible`, 건너뛰기 링크, 입력 label을 유지한다. outline을 대체 없이 제거하지 않는다.
- `transition: all`, 카드 hover 이동, 버튼 클릭 시 과한 축소, 전역 spring·confetti·parallax·무한 로딩 연출을 추가하지 않는다. 애니메이션을 위해 응답을 인위적으로 늦추지 않는다.
- pending에 따른 버튼 폭·레이아웃 튐을 최소화한다. 오류·성공 안내의 `aria-live`가 상태 변경 직후 사라지지 않는지 확인한다.

대비 기준: https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html
경계 기준: https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html
대상 크기 구분: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

### 3.4 한국어 문장과 UI 상태

- 앱이 한 행동과 사용자가 다음에 할 행동을 짧게 쓴다. 구호·추상적인 감성 문구·불필요한 감탄사를 넣지 않는다.
- 평상시: ‘오늘 나누고 싶은 이야기를 적어 주세요.’ / 행동: ‘게시하기’ / 처리 중: ‘게시 중…’.
- 로그인 전: ‘글과 댓글을 남기려면 로그인해 주세요.’와 기존 가입·로그인 링크를 제공한다. 읽기 피드를 가리지 않는다.
- 성공: 실제 확인 후 ‘글을 게시했습니다.’와 기존 결과 링크를 제공한다.
- 입력 오류: 기존 검증의 구체적인 이유를 보존한다. 공백과 한도 오류를 일반 서버 장애로 바꾸지 않는다.
- 결과 미확정: ‘저장 결과를 확인하지 못했습니다. 입력한 내용은 남아 있습니다.’와 기존 재조회 수단을 유지한다.
- 500 코드 포인트 정책과 화면·서버의 공통 정규화를 유지한다. 표시 문구를 줄이면서 한글/이모지 계산 방법을 바꾸지 않는다.
- 기존 계약과 일치하는 문구만 수정한다. 가입 정책·데이터 소유권·숫자·작성자의 실제 글은 디자인을 위해 재작성하지 않는다.

## 4. 빈 다음 페이지 안내를 고친다

대상: `src/app/u/[username]/page.tsx`.

- offset=0의 전체 빈 목록과 offset>0의 현재 페이지 빈 결과를 구분한다.
- 글·followers·following 세 탭에 동일하게 적용한다.
- 첫 페이지의 전체 빈 관계는 ‘아직 팔로워가 없습니다.’, 다음 빈 페이지는 ‘이 페이지에 표시할 팔로워가 없습니다. 처음 목록을 확인해 주세요.’처럼 쓴다.
- 실제 count가 양수인데 전체 관계가 없다고 설명하지 않는다. count나 관계 데이터는 변경하지 않는다.
- 기존 ‘처음으로’ 링크를 재사용한다. 새 페이지네이션 방식·무한 스크롤을 추가하지 않는다.
- 조회 오류를 빈 성공으로 바꾸지 않는다.
- 전체 0 / 전체 1+offset20 / 전체21의 다음 페이지 / DB 오류를 모의 응답으로 검사한다. 공유 DB에 대량 자료를 생성하지 않는다.

## 5. 답글 취소 포커스를 고친다

대상: `src/components/comment-composer.tsx`.

- 열 때 입력창으로 이동하는 현재 autoFocus 동작을 유지한다.
- 취소 후 같은 부모의 ‘답글 쓰기’ 버튼으로 포커스를 되돌린다. DOM이 다시 생기는 시점에 맞춰 ref로 처리하고 임의의 긴 timeout에 의존하지 않는다.
- 초기 렌더에서는 포커스를 빼앗지 않는다. 여러 댓글이 동시에 렌더돼도 다른 부모 버튼으로 이동하지 않는다.
- 취소 후 다시 열면 초안을 보존한다. pending 중 취소 금지, 실제 저장 성공 뒤 초기화, 계정 전환 시 초안 격리를 유지한다.
- Tab → 답글 쓰기 → 입력 → 취소 → 같은 버튼 복귀 → 재열기를 실제 키보드로 검사한다. `document.activeElement`를 확인한다. 입력창 제거만으로 통과 처리하지 않는다.

## 6. SessionBoundary는 먼저 조건부 재현한다

대상: `src/components/session-boundary.tsx`와 사용하는 페이지.

이 항목은 감사 당시 실제 앱에서 재현되지 않은 조건부 위험이다. 확정 결함으로 바꾸어 보고하거나 미리 수정하지 않는다.

재현: A 사용자로 렌더 → 일시적 없음/다른 신원 이벤트로 `changed=true` → 갱신된 서버 화면이 같은 A·같은 key로 도착. 정상 작성 화면으로 복귀하는지 확인한다.

재현되면 검증된 서버 신원과 화면 세대에 맞춰 차단을 해제하는 최소 수정만 한다. 클라이언트 이벤트만 믿고 차단을 풀거나 이전 초안을 다른 계정에 복원하지 않는다. A→B, A→guest, 신원 확인 통신 실패, 이전 쓰기의 늦은 응답을 회귀 검사한다. 미재현이면 조건·결과·남은 한계를 그대로 기록한다.

## 7. 작업 순서와 변경 경계

1. **기준 확보**: git 상태·설정 누락 여부·기존 검사 상태를 확인한다. 가능한 현재 실제 화면을 390px/1440px에서 기록한다. 비공개 raw 자료에 보관하고 비밀·개인정보를 공개 이미지로 만들지 않는다.
2. **기록 안전성**: §2의 가짜 회귀 검사와 export 수정을 완료한다. 원시 기록은 처음부터 유지하고 가림 처리 확인 전 새 공개 export를 만들지 않는다.
3. **디자인 계약**: §3을 `DESIGN.md`에 옮긴다. 기존 AGENTS는 ‘UI 변경 시 DESIGN.md를 읽는다’는 안내만 최소 추가한다. `IMPLEMENTATION_PLAN.md`의 상충하는 시각 기준만 맞춘다. 기능·보안 명세와 과제 원문은 바꾸지 않는다.
4. **첫 화면 수정**: 상단 셸·피드·작성자 행부터 실제 컴포넌트로 적용한다. 별도 static HTML 모형을 완성품으로 만들지 않는다. 같은 데이터·세션·viewport에서 전후를 확인한다.
5. **나머지 화면**: 글 상세·댓글·사람 목록·프로필·인증으로 동일 규칙을 확장한다. §4·§5를 해당 화면 수정에 포함한다. §6은 재현 여부에 맞춰 처리한다.
6. **품질 확인**: 세 스킬의 역할을 구분해 사용하고 §8을 실행한다. 새 기능이나 미감 점수를 위한 재설계는 하지 않는다.
7. **인계**: 실제 결과를 기존 `docs/verification.md`의 P3.5 절에 기록한다. 출처·도구·내보내기를 정리하고 P4 잔여 항목을 남긴다. 코드 반영은 기존 승인 범위에 따른다.

주 변경 파일: `src/app/globals.css`, `src/app/layout.tsx`, 기존 페이지·components, `scripts/export-session.mjs`, 회귀 tests, 선택한 스킬 디렉터리, `DESIGN.md`, 기존 기록·라이선스 문서.

`src/actions/`, 조회 함수, validation, Supabase SSR·proxy, 초기 migration은 원칙적으로 보존한다. 확인된 버그를 고치는 데 필요하면 이유와 정확한 범위를 기록한다. 디자인을 위해 SQL·Auth 설정을 바꾸거나 클라이언트 조회로 전부 옮기지 않는다.

## 8. 완료 검사

### 8.1 기능·상태

- `npm run check`, `npm run build`를 실제 실행한다. 기존 검사·RLS를 완화하지 않는다. 새 테스트가 요구되는 실패를 잡는지 수정 전후로 확인한다.
- `npm run test:access`는 승인된 과제 프로젝트와 준비된 A·B에서만 실행한다. reset·대량 삭제·부하 검사·관리 키 사용·추가 계정 생성은 하지 않는다.
- 승인된 계정으로 글 → 별도 B 조회 → 댓글 → 답글 → 팔로우·해제 → 양쪽 목록·숫자를 확인한다. 기존 점검 자료를 재사용하고 추가 자료의 수량·표식·잔존을 기록한다.
- 실제 21개 이상 pagination이 필요한 상황은 먼저 모의 응답으로 검사한다. 공유 DB에 추가 자료가 필요하면 수량·표식·잔존 여부를 승인받는다.
- DB 도착 전 abort와 저장 후 응답 유실을 구분한다. 후자는 승인된 소량 쓰기 범위에서 실제 저장과 응답 유실을 구분해 관찰한다. 초안 보존·미확정 안내·자동 재전송 없음만 보장하고 멱등성 프레임워크를 추가하지 않는다.
- 새 외부 브라우저의 신규 가입과 외부 서비스 전체 흐름은 공개 배포 후 P4에서 확인한다. 기존 HTTP 가입 증거로 대체하지 않는다.

### 8.2 시각·키보드

| 화면/상태 | 확인할 내용                                                                          |
| --------- | ------------------------------------------------------------------------------------ |
| 전체 셸   | 320·390·768·1440px에서 가로 넘침 없음, 메뉴 줄바꿈, 접근 가능한 이름, 실제 계정 동작 |
| 피드      | 게스트/로그인/빈 목록/불러오기 오류, 긴 한국어·URL, 작성자·본문 정렬, 게시 후 결과   |
| 프로필    | 긴 표시 이름·소개, 실제 두 관계 수·목록, 첫 빈 목록과 다음 빈 페이지 구분            |
| 댓글      | 여러 부모·여러 답글, 펼치기/접기, 페이지·앵커 유지, 입력·취소·재열기                 |
| 폼        | label, 구체적 오류, pending 반복 방지, 성공 전 초안 유지, 실제 OS 한국어 입력기      |
| 팔로우    | 실제 성공·실패와 버튼 일치, 갱신 후 포커스·성공 안내를 잃지 않음                     |
| 접근성    | skip link, Tab/Shift+Tab/Enter, 실제 포커스 복귀, 200% 확대, 동작 감소, 실제 대비    |
| 세션      | A→B·A→guest, 신원 확인 통신 오류, 늦은 응답, §6의 동일 A 복귀                        |

- 브라우저 자동화 도구는 기존 점검 환경을 먼저 재사용한다. 원격 환경에 사용 가능한 브라우저가 없으면 정적 확인과 실제 브라우저 미검증을 구분한다. 스크린샷 확인을 했다고 꾸미지 않는다.
- 대량 데이터·긴 이름 fixture는 모의 검사를 사용하고 실제 점검과 분리한다. 가상 데이터를 앱의 성공 상태에 넣지 않는다.
- 전후 이미지는 같은 route·계정 역할·데이터·viewport로 비교한다. 공개할 이미지에는 비밀·개인정보를 제거하고, 정리된 실제 앱 이미지 또는 표시된 합성 fixture를 구분한다.
- ‘AI 티 제거 완료’ 같은 출처·확률 판정은 하지 않는다. 개선한 정보 순서·문구·정렬·상태·동작과 남은 문제를 기록한다.

### 8.3 인계 결과

기존 `docs/verification.md`에 다음만 남긴다.

1. 기준/결과 commit 또는 현재 미커밋 상태와 변경 파일.
2. export 두 결함의 재현 전후, SessionBoundary 재현 여부.
3. 실제 사용한 OMD 버전·세 스킬·fallback 여부, 채택/비채택 참고 원리.
4. 화면 전후와 route·viewport·계정 역할·실제/모의 데이터 구분.
5. 실행한 명령·종료 결과, 실패와 미검증.
6. P4에서 남은 공개 배포·외부 신규 가입·전체 체험·최종 내보내기 및 제출.

프로젝트 스킬 설치나 P3.5 화면 수정 완료를 P4 배포·제출 완료로 보고하지 않는다. 새로운 장문 계획서를 만들지 않는다.
