# 단풍 — UI 계약

이 문서는 `CODEX_P3_5.md` §3에서 확정한 프로젝트 소유의 독립 계약이다. 과제 원문과 기존 기능·보안 명세가 우선한다. OMD Core v2 graph나 외부 인증을 채택한 문서가 아니다.

## 목적과 참고 범위

한국어 글을 읽고 작성자와 대화를 이어가는 SNS다. 공개 읽기, 로그인한 사용자의 글·댓글·한 단계 답글·팔로우라는 기존 흐름을 보존한다. 상단 탐색에서 본문으로, 작성자에서 글과 댓글로 읽는 순서를 만든다.

[OMD karrot 참고](https://oh-my-design.kr/design-systems/karrot)의 콘텐츠 중심 목록과 [SEED 제품 지침](https://seed-design.io/get-started)의 행동·상태 구분을 참고한다. 카탈로그는 제3자의 분석이며 `verified v2`는 당근의 인증이나 현재 앱 실측 증거가 아니다. 지정된 GitHub commit `15ff013933b37d1800e05c75e7680ed17e56f94b`의 참고 파일은 웹 조회에서 접근하지 못했다. 설치 배포물 2.0.0과 해당 소스의 동일성도 단정하지 않는다. SEED v2, 현행 문서와 마케팅 페이지 수치를 서로 대체하지 않는다.

당근 이름·로고·캐릭터·사진·전용 글꼴·문구, 거래 카드·가격·지역·채팅, 마케팅 랜딩은 사용하지 않는다. SEED 패키지나 타사 HTML/CSS를 설치·복사하지 않는다. 아래 모든 색·치수·배치는 ‘단풍’의 결정이다.

## 색과 형태

| 역할 / CSS 토큰                                           | 값                                                      |
| --------------------------------------------------------- | ------------------------------------------------------- |
| 바탕 `--background` / 표면 `--surface`                    | `#F7F7F5` / `#FFFFFF`                                   |
| 본문 `--foreground` / 보조·placeholder `--muted`          | `#202124` / `#5F6368`                                   |
| 장식선 `--border` / 입력·컨트롤 경계 `--control-border`   | `#E7E5E1` / `#8A8F96`                                   |
| 주 행동 `--primary` / hover `--primary-hover`             | `#202124` / `#343638`, 흰 글자                          |
| 링크·현재 메뉴·포커스 `--accent` / 강조면 `--accent-soft` | `#C2410C` / `#FFF4ED`                                   |
| 오류 `--danger` / 성공 `--success`                        | `#A12622` / `#23633E`, 문구와 함께 전달                 |
| 버튼·입력·선택 메뉴 `--control-radius`                    | `8px`                                                   |
| 아바타 / 글·목록·표면                                     | 원형 / radius·그림자 없음                               |
| 간격                                                      | 4·8·12·16·24·32px                                       |
| 모션 `--motion-color` / `--motion-opacity`                | 색·경계 140ms / 필요한 opacity 120ms, reduce에서는 즉시 |

일반·보조·placeholder 텍스트는 실제 배경에서 4.5:1 이상, 컨트롤 식별 경계·포커스는 3:1 이상을 확인한다. 장식 구분선에 입력 경계 기준을 강제하지 않는다. disabled는 전체 opacity로 텍스트를 흐리지 않고 상태 문구·cursor·disabled 속성으로 표현한다. [텍스트 대비](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html), [비텍스트 대비](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html).

## 글자와 내용

자체 제공 Pretendard와 기존 Arial·Apple SD Gothic Neo·Malgun Gothic·sans-serif fallback 및 OS 이모지를 유지한다. 본문 1rem(16px)·400·1.65, 제목 1.5rem(24px)·700, 이름 1rem·600, 보조 0.8125~0.875rem(13~14px)·400이다. 숫자 카운터는 tabular-nums로 읽는다. 본문은 일반 텍스트·줄바꿈·전체 내용을 유지하고 긴 URL·이름만 필요한 줄바꿈을 허용한다. clamp·HTML 해석·본문 선택 방지는 금지한다.

표제는 ‘전체 글’. 안내는 ‘오늘 나누고 싶은 이야기를 적어 주세요.’, 행동은 ‘게시하기’, pending은 ‘게시 중…’. 게스트는 ‘글과 댓글을 남기려면 로그인해 주세요.’와 기존 가입·로그인 링크를 본다. 실제 확인 후에만 ‘글을 게시했습니다.’를 표시한다. 결과 미확정은 ‘저장 결과를 확인하지 못했습니다. 입력한 내용은 남아 있습니다.’와 기존 재조회 동작으로 구분한다. 서버 검증 사유·500 코드 포인트·정규화·실제 작성 내용·가입 정책을 바꾸지 않는다.

서비스명은 헤더·접근성 이름·메타데이터에 ‘단풍’으로 표시한다. 비밀번호 안내는 ‘8자 이상으로 입력해 주세요.’로, 길이 초과 오류는 영문·숫자 최대 72자와 한글·이모지 포함 시 더 짧아야 함을 함께 안내한다. 글·댓글 카운터는 ‘현재 글자 수 / 500자’, 길이 초과 안내는 ‘500자 이내’로 표시한다. 서버 검증과 결과 상태는 유지하고 화면에서만 ‘세션·저장소·최상위 댓글’ 등의 표현을 풀어 쓴다.

## 셸과 화면별 구성

- 데스크톱 masthead는 ‘단풍’·전체 글/사람들/내 프로필·계정 동작을 한 줄에 둔다. 기존 route·aria-current를 유지하며 AccountMenu는 한 번만 렌더한다. 헤더는 고정하지 않는다.
- 760px 이하에서는 브랜드·계정 동작을 첫 줄, 세 메뉴를 둘째 줄에 둔다. 본문 최대 720px, 기본 좌우 20px, 480px 이하 16px. 오른쪽 패널·슬로건·새 메뉴는 없다.
- 로그인 작성창은 목록 시작부의 평평한 영역이고 textarea 기본 높이는 96px다. 검증·busy 잠금·서버 제출 경계를 보존한다.
- 글은 기존 author 필드로 만든 40px 단색 이니셜(문장부호를 건너뛴 첫 글자, 글자가 없으면 첫 코드 포인트), 이름/사용자명/시각, 본문, 댓글 링크 순이다. 본문과 작성자 정보는 같은 열에 정렬하고 행 전체 링크·중첩 링크를 만들지 않는다. 상세는 같은 컴포넌트의 간격만 조정한다. 프로필 추가 조회는 없다.
- 댓글도 40px 이니셜과 본문을 정렬한다. 부모 아래 답글은 부모 이니셜 중심(40px의 절반인 20px)에 놓인 얇은 연결선과 16px 안쪽 간격을 쓰며 깊이 1, 최신순, thread/replyOffset/댓글 총수와 모든 기존 동작을 보존한다.
- 사람 목록은 이니셜·이름·사용자명·실제 소개의 평평한 행이다. 프로필은 신원·소개·팔로우·세 탭 순서이며 실제 관계 수와 목록만 표시한다.
- 가입·로그인·초기 프로필 폼은 최대 420px다. 이메일 소유권 확인·메일 복구 미제공, 최초 프로필 수정 불가 고지를 유지한다. 장식 옆면·소셜 로그인·표지·배지는 추가하지 않는다.

## 상태·키보드·회귀

주요 버튼·메뉴는 최소 44×44 CSS px를 제품 목표로 삼는다. 이것을 WCAG AA의 일률적 최소치라고 부르지 않으며 인라인 링크는 역할·간격으로 별도 확인한다. [WCAG 대상 크기 설명](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html).

- skip link, label, 2px focus-visible과 4px offset, reduced-motion을 유지한다. hover 색은 hover 가능한 포인터에서만 적용한다. transition: all·카드 이동·클릭 축소·인위적 응답 지연은 없다.
- pending 버튼의 폭을 유지하고, 실제 성공·실패 안내와 aria-live를 보존한다. 실패 초안·취소 후 재열기 초안·계정 간 격리를 유지한다.
- 답글 입력의 autoFocus는 유지한다. 취소 후 새로 렌더된 같은 부모의 답글 버튼에 ref로 포커스를 복원한다. 초기 렌더에는 포커스를 이동하지 않는다.
- 프로필의 첫 빈 목록과 offset>0 빈 페이지를 구분한다. 기존 처음으로 링크를 쓰며 조회 실패를 빈 목록으로 숨기지 않는다.
- SessionBoundary는 재현 후에만 수정하며, 신원 이벤트 자체로 차단을 풀지 않는다. 검증된 서버 신원·화면 세대가 돌아온 경우만 복귀를 허용한다.
- 320·390·768·1440px, 200% 확대 상당 reflow, 실제 대비·키보드·실패·세션을 확인한다. OS 한국어 입력기처럼 자동화하지 못한 검사는 미검증으로 남긴다.

## 변경과 검증의 책임

CSS 역할 토큰은 `src/app/globals.css`에 모은다. 기능·권한·쿼리·SQL을 디자인 목적으로 바꾸지 않는다. omd:apply는 이 계약 적용, omd:feel은 수치·포커스·상태 점검, omd:slop-audit는 같은 데이터·route·viewport 전후의 정보 위계 점검에 사용한다. 휴리스틱을 규격으로 승격하거나 맛 점수·AI 확률을 만들지 않는다. 결과와 미검증은 기존 `docs/verification.md`에 기록한다.
